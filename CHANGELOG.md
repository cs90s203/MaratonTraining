# CHANGELOG

版本號規則：功能完成 = 中位數，bug fix = 末位數。每個 Checkpoint 完成時 bump 中位數。

---

## v0.18.0 — 2026-09-14 · 課表上點名稱就從項目庫換、只改時間

見 [決策紀錄第 45 條](docs/決策紀錄.md)。

- 教練模式的課表項目沒有「編輯」表單了：點項目名稱，直接從項目庫挑一個換掉（打過的勾不受影響）。
- 時間（長跑是公里）那格可以直接改，改完就存。
- 「存成常用」只在跟項目庫不一樣時出現（出廠的項目、改過時間的），按了直接存，卡片寫「已存進項目庫」。
- 一天最下面「＋ 從項目庫加一個」。今天以前的日子唯讀。
- 項目庫的表單精簡：一個「名稱」、時長／距離／RPE 各一行、拿掉強度說明跟長說明。
- 動作清單的份量改成一行「5 組 × 30 秒」，可以同時設組數跟秒數；新動作不再預填「8–10 次」，次跟秒切換時清掉數字。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.17.1 — 2026-09-14 · 安全提醒按鈕搬到本週訓練目標卡；拿掉雙層圈

- 週次標題旁的「！」安全提醒按鈕，改放到「本週訓練目標」卡右上角。
- 按鈕本身的外框圈拿掉了——圖示自己就畫了一圈，兩層圈疊在一起改成一圈。

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

---

## v0.17.0 — 2026-09-14 · 課表項目先挑常用項目、只調數字

見 [決策紀錄第 44 條](docs/決策紀錄.md)。

- 教練模式「＋ 新增項目」先從常用項目挑一個，一次帶入類型、心率、RPE、訓練段落、影片、動作；沒有合適的才用空白項目。
- 課表上的項目表單變簡單：上面只有時長、距離、備註，加一行「包含什麼」；其餘收進「更多設定（通常不用改）」。
  存檔被擋時會自動打開「更多設定」。
- 常用項目庫的表單照舊完整，是設定項目內容的地方。
- 教練工具列的「編輯」在手機上不會再折成兩行。
- 「產後核心／骨盆底啟動」動作清單底下的說明改成只寫「組數次數依身體感覺調整即可」。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.16.0 — 2026-09-14 · 教練模式也能拖曳；項目庫可以新增、複製

見 [決策紀錄第 43 條](docs/決策紀錄.md)。

- 教練模式的本週頁也有 ⋮⋮ 把手，拖曳搬的是**所有人的共用課表**。只限還沒開始的週：已經開始的週搬動會讓已經打的勾對不上、
  做過的課在後面的日子又出現一次，所以不給拖，並說明「要換自己這週的順序，關掉教練模式再拖」。一般模式照舊是自己的順序。
- 設定頁「常用項目庫」多「＋ 新增常用項目」：直接在庫裡建跑步（或任何）項目，表單裡有訓練段落跟「套用間歇範本」。
- 常用項目、動作清單、影片每一列都有「複製」（內建的也可以），打開一份預填好的「（複本）」，按存檔才建立、取消什麼都不留。
  從內建動作清單複製出來的，固定的安全提醒照樣跟著，改內容也拿不掉。
- 兩位教練同時改同一週：一個人搬了天數，另一個人還停在舊畫面的修改不會存到錯的那一天（會請她再改一次）。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.15.0 — 2026-09-14 · 跑步項目可以寫訓練段落（間歇跑）

見 [決策紀錄第 42 條](docs/決策紀錄.md)。

- 教練模式項目表單多「訓練段落（選填）」：暖身／主段／恢復／技術動作／緩和，數量（分、秒、公尺、公里）、心率 Zone、說明；
  可以加「重複 N 次」的組。有「套用間歇範本」：暖身 10 分 → 4 ×（400 公尺＋恢復 90 秒）→ 緩和 10 分。
- 卡片上一段一行顯示，重複組寫「4 ×」。
- 出廠課表 W9-16 跑姿訓練日的「各 2 組 x 20 公尺」改寫成段落（備註的文字照舊留著）。長跑的 MP 段、節奏跑、喚醒跑
  原文沒有具體數字，沒有替你補，請用段落編輯器排。
- 編輯器底下即時顯示段落合計；「800 分」這種忘了換單位的數字、空的重複組會擋下並說明；刪整組重複會先確認。
- 表單開著時，同步進來的更新不會洗掉還沒存的內容。
- 段落不算週跑量，週跑量照舊用項目的總時長／總距離。常用項目會連段落一起存。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.14.1 — 2026-09-14 · 登入後自動選成那個帳號的人

見 [決策紀錄第 41 條](docs/決策紀錄.md)。

- 用 Google 帳號登入後，「我是誰」自動選成那個帳號的人（mick／Annlin／Phoebe），不用再手動切。
  判斷方式是問 Firebase 的規則（誰讀得到自己的身體狀況），程式碼裡沒有放任何人的 email。
- 離線或判斷不出來時維持原本選的。設定頁會在偵測到的人旁邊標「登入的帳號」。
- 新手機登入前記的紀錄會一起搬到登入的人名下並上傳；共用裝置登出時清掉那個人的身體狀況快取；手動切回正確的人時「身分不符」會消失。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.14.0 — 2026-09-14 · 內建內容可以刪除；新增間歇跑類型

見 [決策紀錄第 39、40 條](docs/決策紀錄.md)。

- 設定頁常用項目庫：4 份內建動作清單、8 支內建影片都有「刪除」。刪除是從常用項目庫跟下拉選單拿掉，
  已經排進課表的日子照樣顯示；刪掉的列在「已刪除的內建」，可以「恢復」。要先登入。
- 教練模式項目表單的「類型」多一個「間歇跑」，算跑步（週跑量目標、實際公里）。只加類型，沒有排進課表、沒有預設強度。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.13.2 — 2026-09-14 · 今天以後的日子也能填實際公里／分鐘

見 [決策紀錄第 38 條](docs/決策紀錄.md)。

- 本週頁今天以後的日子也有「實際公里」「實際分鐘」填寫框（v0.10.0 起未來的日子不顯示，看起來像欄位不見了）。
  今天以前的日子不變。
- 未來的日子填了數字**不會自動打勾**，當天按「完成」才算完成；填的公里會算進那週的實際跑量。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.13.1 — 2026-09-14 · 本週頁展開的那一天整塊一個底色

見 [決策紀錄第 37 條](docs/決策紀錄.md)。

- 本週頁展開的那一天，底色從標題列延伸到整塊內容，看起來是同一天的東西。
- 「實際分鐘／公里」的填寫框跟「更換項目｜自主休息」之間拉開距離。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.13.0 — 2026-09-14 · 教練可以改內建的動作清單跟影片

見 [決策紀錄第 33–35 條](docs/決策紀錄.md)。

### 新增
- **內建的動作清單（4 份）跟影片（8 支）可以在 App 裡改。** 設定頁常用項目庫列出內建內容，標「內建」或
  「內建·已修改」，寫出用在幾天；本週頁教練模式展開「查看動作」可以直接「編輯這份動作清單」。
- **改動只從今天起生效，今天以前的日子維持原樣。** 「還原內建」也一樣只從今天起。自訂的動作清單跟影片也改成這樣。
- 改內建內容要先登入；兩個人同時改同一份，後存的人會被擋下並提示。存不進去時畫面會退回改之前。
- 編輯器：動作可以上下移；備註跟負荷說明改成多行、限制字數；影片可以選「不需要連結」。
- 「查看動作」最上面顯示負荷說明；每份內建清單有一段**固定的安全提醒**（漏尿、下墜感、疼痛就停），改清單也改不掉。

### 改變
- 影片前面的圖示改成實心圓播放鍵，跟「查看動作」前面的 ▶ 分得開。
- 點進打字框不再自動放大畫面（viewport 加 maximum-scale=1）。見決策紀錄第 36 條。
- 沒登入時，本週頁最上面提示「看到的是出廠課表」並附登入按鈕——Safari 分頁跟主畫面 App 的登入狀態不共用，
  沒登入就看不到教練改過的內容跟自訂影片。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.12.1 — 2026-09-14 · 影片 ▶ 圖示、正式 App icon

見 [決策紀錄第 32 條](docs/決策紀錄.md)。

- 影片連結前面的 ▶ 圖示原本一直被擠成 0 寬、看不到，現在看得到了；搜尋型的影片也改用 ▶。
  設定頁常用項目庫的影片清單也加上 ▶。
- 換上 TOKYO 2027 跑者 App icon（主畫面、分頁小圖示）。新增 `tools/make_icons.py` 從設計圖產生所有尺寸。
  **已經加到主畫面的 iPhone 要移除再重新加入**，才會換成新圖示。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.12.0 — 2026-09-14 · 多部影片、心率改用 Zone、打勾誤觸修正

### 新增：一個項目可以掛好幾部影片（[決策紀錄第 28 條](docs/決策紀錄.md)）
- 教練模式的項目表單：影片參照一部一列，底下「＋ 再加一部影片」，每列旁邊 ✕ 拿掉。最多 10 部。
- 卡片上每部影片一個連結，連結上直接寫影片名稱。
- 舊資料（只有一部影片）照常顯示，不用重選。

### 改變：心率區間一律用 Zone（[決策紀錄第 30 條](docs/決策紀錄.md)）
- 課表跟畫面上的「50-60%／60-70%／65-72%／70-75%」換成 Zone 1／Zone 2／Zone 2-3／Zone 3。
- 教練表單的心率區間改成下拉選 Zone。教練改過的週還存著舊的百分比，畫面自動換算成 Zone 顯示。
- 長跑（Zone 2-3）跟節奏跑（Zone 3）的心率上限變成 80%——這是 Zone 的精度，RPE 跟強度說明沒變。
- 本檔較早的條目、決策紀錄、日曆基準裡的百分比也一起改寫成 Zone。恢復跟 Zone 2 換算前後一樣；長跑、節奏跑、MP 換成 Zone 之後範圍變寬，原本的百分比留在決策紀錄第 30 條的對照表。

### 修正
- 點開「查看動作」一秒後自己收起來——開關狀態現在記在 App 裡，重繪不會收；常用項目庫的訂閱
  也不再因為「快取 → 伺服器確認」這種沒有內容變動的快照整頁重畫。（[第 29 條](docs/決策紀錄.md)）
- 只有圓圈能打勾：教練模式的項目卡、一般模式二擇一的選項卡原本點框的任何地方都會觸發。
  圓圈的點擊範圍往外放大，二擇一的圓圈也放大。（[第 29 條](docs/決策紀錄.md)）
- 未來的日子被誤觸打勾之後改不回來：未來的日子現在不能預先打勾（休息可以），已經被打勾的
  「完成」按鈕照樣出現、點一下就取消。（[第 31 條](docs/決策紀錄.md)）
- App 整晚開著、隔天早上切回來，今天的圓圈還是昨天畫的停用狀態——日期一換就重畫。
  選中的二擇一圓圈點擊範圍也不再比沒選中的小。（對抗式審查抓到，[第 29、31 條](docs/決策紀錄.md)）

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.11.1 — 2026-09-14 · 總覽頁的階段條重畫

見 [決策紀錄第 27 條](docs/決策紀錄.md)。

- 「我的進度」的階段條改成一週一格的時間軸，照階段分組：過去的週填淡色、這週深色而且比較高、
  未來是底色；階段名稱寫在格子下面，目前的階段加粗。
- 卡片上面寫目前階段跟「本階段第幾週／共幾週」，下面寫下一階段從第幾週開始。
- 修正手機上階段條要左右捲、「賽週」被切掉。
- 拿掉跟階段條重複的整體進度條。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.11.0 — 2026-09-13 · 教練模式的常用項目庫

見 [決策紀錄第 26 條](docs/決策紀錄.md)。

- 教練模式的項目卡多一顆「存成常用」：把一個項目的整套設定（類型、標題、時長、距離、心率、
  RPE、影片、動作清單、備註）存成常用項目。
- 「＋ 新增項目」表單最上面可以「從常用項目帶入」。**帶入是複製**：之後改範本不會改到已經
  排好的日子。
- 設定頁（教練模式開著時）新增「常用項目庫」，三人共用：
  - 常用項目：改名、改內容、刪除
  - 動作清單：可以新增自己的（每個動作填組數、次數或秒數、是否每邊、備註）
  - 影片：可以新增搜尋關鍵字，或貼上 YouTube 網址（只收 https://）
- 自訂的動作清單和影片會出現在項目編輯的下拉選單（標「自訂」）。刪除只是從選單拿掉，
  已經排進課表的照樣顯示。
- 新的 Firestore 集合 `library`：**需要把 `firestore.rules.local` 重新貼到 Firebase Console
  發布**。還沒發布時只有這個功能讀不到（設定頁會提示），不會把人登出。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.10.1 — 2026-09-13 · 修正 iOS 數字欄位縮放縮不回去

見 [決策紀錄第 24 條](docs/決策紀錄.md)。

- 填數字時 iOS Safari 自動放大畫面、切走後不會自動縮回去——照搬 babylog 已經驗證過的
  `resetZoom()`（強制把 viewport 的 maximum-scale 壓到 1.0 再還原），並修正短時間內
  連續呼叫會把縮放永久鎖死的問題（改用單一計時器，只在第一次呼叫時記原始內容）。

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

---

## v0.10.0 — 2026-09-13 · 當天紀錄卡重排：課表併進來、數字排第一、拿掉「錯過」

見 [決策紀錄第 23 條](docs/決策紀錄.md)。

- 展開的一天不再是「項目卡＋實際操作卡＋體感強度＋身體狀況卡」四塊拼裝，改成
  **一張紀錄卡**：課表項目當標題，底下照「運動回來先記什麼」排——實際公里／分鐘＋
  完成 → 沒照表？更換項目｜自主休息 → 體感強度 → 附註 → 身體狀況。子區塊用分隔線區分，
  不是各自的外框／底色。
- **填了公里或分鐘就自動完成**（單一項目的日子）——不用再多點一下打勾；清掉數字不會
  取消完成，要取消請點「完成」按鈕。
- 按鈕文案：改做 → **更換項目**、主動休息 → **自主休息**。**拿掉「錯過」**——它在完成率、
  週跑量目標裡的算法本來就跟「什麼都沒按」一樣，數字不受影響；過去沒記錄的日子改顯示
  「未完成」（不是「待完成」，第 0 條：對過去的日子不催促）。舊文件裡的 `missed` 讀成
  照表，資料不用改。
- 課表二選一、其中一個是完全休息的日子，不重複放「自主休息」——課表本身已經是休息選項。
- 修正：未來的日子原本會冒出可以預先打勾的「完成」按鈕，現在跟數字欄一起隱藏
  （決策紀錄第 13b 條：未來只能預先排休息）。
- 身體狀況的「＋備註」改「＋寫給自己」。

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

---

## v0.9.0 — 2026-09-13 · 階段性目標：Zone 2 配速、跑量、5K 技術指標

見 [決策紀錄第 22 條](docs/決策紀錄.md)。

- 總覽頁新增「階段目標」卡，依訓練階段（恢復奠基期／基礎期／賽前期／減量期／賽週）
  各自設定：Zone 2 配速目標、跑量目標、5K 技術指標（Cadence 步頻／Vertical Oscillation
  垂直振幅／Ground Contact Time 觸地時間／Stride Length 步幅）。
- 跑量目標有「本階段累積實際 vs 目標」比對（沿用週跑量算法逐週加總）；旁邊顯示課表
  這階段自動算出的參考量，教練目標超過時提醒（不阻止存檔）。其餘五項純目標、不比對實際。
- 配速旁顯示該階段 Zone 2 對應的心率／RPE 區間當背景參考。
- 這六個數字刻意**不設自動上限**（跟週跑量目標不同）——教練的專業判斷，App 只顯示
  參考不擋存檔。配速輸入接受 mm:ss（例如 6:30）。
- 資料存在 `users/{userId}/profile/phaseTargets`，跟訓練目標同一套權限（本人隨時可編輯，
  教練模式下可幫別人設）。

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

---

## v0.8.2 — 2026-09-13 · 體感強度拿掉高低階梯，改成等高＋四色

見 [決策紀錄第 21 條](docs/決策紀錄.md)。

- 體感強度四組（輕鬆／中等／困難／全力）拿掉「越吃力越高」的階梯圖，改成**等高**，
  改用底色分輕重：輕鬆綠、中等黃、困難橘（新增）、全力紅，沿用 App 既有的
  accent/warn/crit 色，只多一個困難用的橘色。
- 選中的那一格改成實心＋深色外框（不再靠變大變高）；課表建議的 RPE 區間改成外框
  而非填色，避免跟等級本身的顏色打架。

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

---

## v0.8.1 — 2026-09-12 · 教練編輯表單拿掉兩個勾選框；「寫入被拒」點得出原因

見 [決策紀錄第 19 條](docs/決策紀錄.md)。

- 教練模式的項目編輯表單最底下「強度是內插值／內容是推導值」兩個勾選框拿掉——它們是轉檔
  留下的「還沒有人確認過」標記，不是教練要填的。教練存檔＝確認過，兩個標記一律清掉，
  標題旁的「推導值」「（內插）」標籤跟著消失（二擇一的日子兩個選項一起清，不會只剩一邊有標籤）。
  被編輯的項目本來有標記時，表單最上面一行會講清楚。手機上兩個標籤文字直排的版面問題隨之消失。
- 同步膠囊「寫入被拒」改成「寫入被拒，點擊看原因」——手機沒有 hover，原本的原因看不到。
  訓練目標寫入被拒時改講真正的原因（Firebase 上的規則可能還是舊版，要重新貼上發布）。
  修正：被拒之後膠囊會被下一次快照洗回「已同步」，現在會停在「寫入被拒」直到那些寫入補上。
- `firestore.rules`（與本機的 `.local`）：userId → email 對照從連鎖 `?:` 改成純 `&&`／`||`
  （照 babylog 的寫法），Firebase 規則編輯器對第三層 `?:` 標紅點。語意不變，
  **本機的 `firestore.rules.local` 要重新貼到 Firebase Console 發布**。見決策紀錄第 20 條。

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>

---

## v0.8.0 — 2026-09-12 · 本週順序改成拖曳

設計裁決見 [決策紀錄第 18 條](docs/決策紀錄.md)。

- 本週頁每列右邊多一顆 ⋮⋮ 把手，**按住拖曳**就能換這週的順序（插入語意，跟 iOS 清單一樣）；
  「本週順序」那張兩個下拉選單的卡片拿掉。拖回出廠順序等於還原。
- 清單下方一行提示；對調過之後顯示「已對調：…」＋「還原順序」。
- 用 SortableJS（cdnjs）。沒載到（離線、CDN 被擋）時把手不出現，其他功能照常。
- 週日回顧多一行：這週有幾天體感比課表要求吃力——只提醒下週不要加，不建議補。
- 修正：畫面渲染失敗的復原按鈕還指向已拿掉的「今日」頁。

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>

---

## v0.7.0 — 2026-09-12 · 拿掉「今日」頁，本週頁改手風琴；改做類型；體感強度

設計裁決見 [決策紀錄第 17 條](docs/決策紀錄.md)。

### 頁面
- **沒有「今日」頁了**。本週頁七列改成手風琴，一次展開一列，列身就是原本今日頁的內容
  （訓練說明 → 實際操作 → 身體狀況）。開 App／按「本週」時今天那一列自動展開並捲到可見。
  導覽列變三格。表格模式留著。
- 安全提醒收進本週標題旁的「！」按鈕（點開全文彈窗，四段一段不少）；跑量卡的說明收進「？」。
- 身體狀況：三顆旗標常駐，備註框有旗標／有內容／點「＋備註」才展開。
- 修正：底部最後一張卡片被導覽列蓋住——`#root` 的底部留白沒算 iPhone home indicator 的安全區。

### 新增紀錄欄位
- **改做類型**：點「改做」出現第二排（跑步｜重訓｜核心｜騎車｜游泳｜走路｜其他），
  存 `entries.substituteType`。週跑量從此**只算改做「跑步」的公里**；改騎車時公里欄不顯示、
  已填的清掉。週視圖顯示「改做·重訓」。
- **體感強度**：Apple Fitness「Rate Your Effort」的四階梯（1-3 輕鬆／4-6 中等／7-8 困難／
  9-10 全力），存 `entries.effort`。課表要求的 RPE 用淡色點標在階梯上；**只在打得比課表高時
  提示**（第 0 條：吃力是警訊，不是目標）。錯過／主動休息不評。

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>

---

## v0.6.3 — 2026-09-12 · 登入改回跟 babylog 一模一樣；v0.4.2～v0.6.2 三次修法全部撤回

使用者糾正：babylog 與日文學習 App 同一套架構（GitHub Pages + firebaseapp.com
authDomain + compat SDK 12.17.0 + iOS「加入主畫面」）登入、同步都正常——v0.6.2 斷定
「主畫面模式無法登入」是錯的，而且把原本能用的模式整個擋掉；v0.6.1 之後連 Safari 分頁
也登不進去。三個版本的修法都是根據網路搜尋推論出來的假設，沒有一次先比對 babylog
的程式碼。完整對照表見 [決策紀錄第 16 條](docs/決策紀錄.md)。

### 撤回
- v0.4.2 的「Safari 改用 redirect」、v0.6.1 的「iOS/Firefox 一律 redirect」＋逾時計時＋
  sessionStorage 旗標、v0.6.2 的「主畫面模式不讓登入」＋說明卡片，全部移除。

### 改回
- `Sync.signIn()` 一字不差照 babylog 的 `signInWithGoogle()`：popup 優先，只有
  `auth/popup-blocked` 才退回 redirect，`auth/popup-closed-by-user` 回 idle。
  不做任何瀏覽器／裝置判斷。
- 帶進 babylog 2026-08-02 事故的 `experimentalAutoDetectLongPolling`（同一支手機、同樣
  的行動網路：串流連線被中間設備弄斷時 SDK 自動退回 long-polling）。

### 保留（只讓失敗看得見，不改登入機制）
- 頂欄小標籤在「沒登入」時也會顯示「登入中…」（可再點一次重開視窗）、「登入失敗，
  點擊重試」、「未授權」——之前這三個狀態排在「沒登入 → 點擊登入以同步」之後，永遠
  顯示不出來。
- 未授權時訊息寫出被拒絕的 email，且登出後狀態不洗回 idle（babylog 也這樣做）——
  這是「帳號不在白名單／規則改了沒發布」跟「登入壞了」唯一分得出來的地方。

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>

---

## v0.6.2 — 2026-09-12 · 找到真正的成因：Safari「加入主畫面」模式（**已撤回，見 v0.6.3**）

v0.6.1 上線後使用者回報還是一樣，並補了關鍵資訊：**用的是 Safari 加入主畫面的圖示**，
不是在 Safari 分頁裡開。這徹底改變診斷方向——查證後確認這不是「該用 popup 還是
redirect」的問題，是這個環境本身：

1. **儲存空間跟 Safari 分頁是分開的兩個 partition**：從主畫面圖示開啟的網頁應用程式，
   跟同一個網址在 Safari 分頁裡打開，iOS 當成完全不同的儲存空間。就算登入真的完成，
   Firebase Auth 的 session 也不會出現在主畫面模式這邊，反過來也一樣。
2. **Google 的登入頁會拒絕嵌入式 webview**：這是 Google 自己的防釣魚政策
   （`disallowed_useragent`），從主畫面圖示開啟的網頁，從 Google 的角度就是一個
   webview，不是「瀏覽器」——不管換 popup、redirect，或之後可能導入的 Google
   Identity Services，都要透過同一個 Google 登入頁，一樣會被擋。

這兩點合起來代表：**問題不在我們呼叫哪個登入 API，在於這個環境本身不支援**，
v0.6.1 猜測的「換一種登入方式」方向本來就不可能解決這一種情況（它解決的是另一種：
一般瀏覽器分頁裡跨網站資料被擋的問題，那個修正保留，對用 Safari 分頁的人依然有效）。

新增 `Sync.isStandaloneHomeScreenApp()`（讀取 iOS 自己的 `navigator.standalone`），
偵測到主畫面模式時**不嘗試登入**（沒有意義，只會卡住或逾時），改成直接說明成因並
給出唯一解法：改用 Safari 分頁打開同一個網址登入，本機已經記錄的紀錄不會遺失。
設定頁在按登入之前就會主動顯示這個說明（不用先失敗一次才看到），並提供「複製網址」
按鈕方便貼到 Safari。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.6.1 — 2026-09-12 · 修正登入後畫面還是跟未登入一樣（v0.4.2 沒有真的修好）

使用者回報：登入之後，畫面還是跟沒登入時一樣。查證後發現 v0.4.2 的修法本身有兩個洞，
剛好疊在一起讓症狀維持不變：

**成因一：`isSafariBrowser()` 排除了 iOS 上的非 Safari 瀏覽器，但它們一樣受 ITP 限制。**
蘋果規定 iOS 上所有瀏覽器都要用 WebKit（App Store 政策），Chrome/Firefox on iOS 只是套了
自己介面的 Safari，一樣會被 ITP 擋掉彈出視窗登入結果的跨網站傳遞——但舊版的判斷式把
UA 含 `crios`／`fxios` 的瀏覽器排除在「需要整頁跳轉」之外，導致 iPhone 上用 Chrome 或
Firefox 登入，會走進跟原本 Safari 一樣會卡住的 popup 路徑。改成判斷「引擎是不是一定會擋
第三方資料」（iOS 不分瀏覽器名稱、桌機 Safari、Firefox 不分平台），不是判斷瀏覽器名稱。

**成因二（更根本）：卡住的登入完全沒有任何畫面反應。** `renderSyncPill()` 的第一條規則是
「沒登入就顯示『點擊登入以同步』」，這條規則排在所有狀態判斷之前——`signInWithPopup`
卡住不拋錯也不 resolve 時（第三方資料被擋的典型行為），`Sync.state` 停在 `'signing-in'`
但畫面完全不看這個狀態，直接顯示「點擊登入以同步」，跟從沒按過登入一模一樣。同樣地，
Firebase 官方文件證實：2024 年中起，Chrome 115+／Firefox 109+／Safari 16.1+ 在沒有額外
設定時，`signInWithRedirect` 導回來也會正常 resolve 成「沒有使用者」而不是拋錯——這條
路徑舊版完全沒有處理。

修法兩層：
1. 登入逾時保險（12 秒）：不管走 popup 還是 redirect，逾時前沒有變成已登入也沒有任何
   錯誤，一律轉成看得見的失敗狀態，不能讓畫面跟「從沒登入過」一樣。
2. `sessionStorage` 記住「剛剛送出過一次 redirect 登入」，撐過整頁跳轉；回來後如果
   `getRedirectResult()` 是「沒有使用者」，用這個旗標判斷「這是不是剛剛登入失敗」，
   而不是把它當成一般的「沒有登入過」。
3. `renderSyncPill()`／設定頁補上「登入中…」「登入失敗，點擊重試」兩個狀態的畫面——
   之前這兩個狀態存在於 `Sync.state`，但因為排在 `!isSignedIn()` 判斷之後，永遠不會被
   看到。`A.retrySync()` 在「還沒登入」時改成重新觸發登入（原本只會重新訂閱，登入沒完成
   時什麼都不做）。

用 node harness 模擬「popup 卡住不 resolve」與「redirect 回來後拿到空使用者」兩種
Firebase 官方文件記載的失敗模式，確認都會在有限時間內轉成看得見的失敗狀態，不留在
跟未登入一樣的畫面上；也確認一般沒登入過的頁面載入不會被誤判成失敗。

**如果這次還是沒有完全解決**：Firebase 的官方建議是這類第三方資料限制唯一真正根治的
方法要嘛是自己的網域（GitHub Pages 的 `github.io` 網址不算），要嘛換成 Google Identity
Services（不經過 Firebase 的中繼頁面）——後者需要 Firebase Console →
Authentication → Sign-in method → Google → Web SDK configuration 裡的「Web client ID」，
之後有需要再取。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.6.0 — 2026-09-12 · 本週順序對調 + 教練模式可幫別人設目標

設計裁決見 [決策紀錄第 14、15 條](docs/決策紀錄.md)。

### 新增：本週順序對調（環境因素讓某天跟另一天對調）

週視圖新增「本週順序」卡片（教練模式關閉時才顯示），可以選兩個星期對調，課表項目
內容完全不變，只是這週哪個日曆格子顯示哪天的內容——因為是純排列，不可能變相加量。
被對調的兩天會標「對調自 O」小標籤（今日視圖、週視圖卡片／表格都有）。教練模式開著
時一律當作沒有對調（那個模式編輯的是共用課表結構，需要日曆格子跟出廠天一一對應）。
`weekAdjustments` 的本機快取從「只存自己」改成 `{userId: {...}}`，並新增
`Sync.subscribeOtherWeekAdjustments`，讓總覽頁算別人的完成率／週跑量時能用「那個人
自己」的對調順序，不會拿錯的內容去對他的打勾紀錄。

### 新增：教練模式下可以互相設定訓練目標

沿用決策紀錄第 11 條的哲學（三人皆可，不限定一人是教練）：教練模式開著時，任何白名單
成員都能編輯任何人的訓練目標，不限本人。`firestore.rules` 的 `profile` 寫入權限從
`isSelf(userId)` 放寬成 `isMember()`（跟 `planOverrides` 同一個模式）——**這是規則變更，
上線前要到 Firebase Console 重新貼上 `firestore.rules.local` 並發布**。教練幫別人設的
目標顯示「教練模式編輯」標籤，且只存在記憶體，不寫進教練自己裝置的本機快取。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

---

## v0.5.0 — 2026-09-11 · 舊 Notion 課表格式整合：實際操作、四態狀態、週跑量目標、訓練目標；走跑改 Zone 2

設計裁決見 [決策紀錄第 12、13 條](docs/決策紀錄.md#12-課表裡沒有走跑phase-1-跑步日改-zone-2-跑強度-50-60--60-70使用者裁定)。
實作前先跑了一輪三鏡頭（第 0 條／資料同步／手機 UX）的對抗式設計審查，
改掉了原設計的四個方向錯誤（見下方「設計審查改掉的」）。

### 課表：沒有走跑了（決策紀錄第 12 條）

使用者裁定「走跑太含糊，應該是 Zone 2 跑」。`walk-run` 類型從產生器、驗證腳本、教練模式下拉
全部移除；Phase 1 週二／週五與 W1-6 長跑改成 Zone 2 跑，時間數字不變。W1-6 長跑刻意
停在 Zone 2、不跳到長跑慣用的 Zone 2-3。**這是 21 個未過期日子的強度上升（Zone 1 → Zone 2）**，
是使用者知情的裁定，不是轉檔加量。`verify_plan.py` 47 項（+6）全過，新增「沒有 walk-run」
「標題／備註不含走跑」「長跑強度：W1-6 Zone 2、W7 起 Zone 2-3」「Phase 1 的 run 一律 Zone 2」等守衛。

### 新增：每天一個「實際操作」方塊

- 三顆狀態 chip：改做／錯過／主動休息（再按一次取消）；「照表」是右上角的唯讀文字，不是可按的選項。
  打勾與狀態互斥，由 `Store` 維護：打勾＝清 status，設狀態＝清勾（錯過／主動休息連實際數字也清）。
- 實際分鐘／公里從每張項目卡搬到這裡（一天一筆的欄位終於畫在一天一個的地方——之前
  一天兩個有時長的項目會畫兩個輸入框綁同一個值）。公里欄在有跑步項目或「改做」時出現。
- `actualNote` 自由文字（一起練的人看得到）；私密的「身體狀況」欄從收合改成預設展開，
  兩個框的標題直接寫「Annlin、Phoebe 看得到」／「🔒 只有你看得到」。
- 純休息日不放狀態 chip；未來的日子只能預先排「主動休息」。

完成率（照表完成率）的分母：拿掉主動休息**與改做**——改做是第 0 條唯一允許的補做方式，
算成跟錯過同分會把人推向硬照表操課。錯過留在分母。

### 新增：週跑量目標 vs 實際（取代決策紀錄第 8 條）

目標不再存 `plan.json`，由課表跑步項目即時加總（教練改項目自動跟著變）；只有時長的項目用
9 分速換算（原文 8-9 分速的慢端，換出來偏低）；比賽日另計；主動休息、改做沒記公里的日子不算；
已標記降量的週不比對。實際＝各天公里加總，照表的日子只填分鐘也會換算（標「約」）。
進度條的 100% 點是目標**下限**，超過上限改成警示文字，不畫「多出來」。
教練模式可以手動設週目標，但**上限不能高於課表加總**——數字只能往下調。
本週頁一張跑量卡，總覽頁一張 26 週「目標區間 vs 實際」圖。

### 新增：我的目標、週視圖表格模式

- 總覽頁「我的目標」：比賽目標一句＋自訂目標清單（文字＋達成勾），存 `users/{userId}/profile/goals`，
  一起練的人看得到、只有本人可寫。只收文字、不畫進度、不跟跑量連動。
- 週視圖「表格（課表｜實際）」模式，模仿舊 Notion 表格給回顧用；本機記住切換。

### 設計審查改掉的（實作前）

- 換算配速原本寫 8 還註解成「偏低」——方向反了（公里＝分鐘÷分速，數字越小目標越高），
  W7-8 會算出 10-11.9K 超過原文自己的 8-10K。改 9，verify 守 ≥ 9。
- `race` 原本算進週跑量：賽週目標 47K+、進度條整週停在 10%、26 週的圖被壓扁。改成另計。
- 「照表」原本是第四顆可按的 chip：按了沒反應、二擇一日無法定義。改成唯讀文字。
- 改做原本跟錯過同分（留在分母不進分子）。改成跟主動休息一樣移出分母。
- 主動休息／降量週的跑量目標原本不縮。改成跟完成率分母同一套。

### 順帶修掉的既有 bug

- **`firebase-sync.js` 綁 `_cloudPushPlanOverride` 時少接第三個參數 `baseUpdatedAt`**，版本比對永遠拿到
  `undefined`——同一週第二次教練編輯必定被判成「剛被別人改過」而丟掉；`_cloudDeletePlanOverride`
  同樣少接 `backup`，還原失敗永遠不回滾（v0.4.1 宣稱修好的那條其實沒接上）。改用 `bind`。
- 遠端文件寫回本機從整份 last-write-wins 改成逐欄位合併：兩台裝置分別改同一天的不同欄位時，
  舊做法兩台各只看到自己的欄位，且永遠不會自我修正。
- 總覽頁未登入就訂閱別人的 entries、失敗後被記成「已訂閱」，登入後不再重訂。
- `Number(null) === 0`：週實際本來會把「沒填」算成 0 公里（在測試時抓到）。

`schemaVersion` 4：`plan.json` 拿掉 `weeklyVolumeKm/weeklyVolumeNullReason`、新增 `timeBasedRunPaceMinPerKm`；
`entries` 新欄位是向後相容的加法，舊快取不需遷移。

### 實作後又追加一輪程式碼審查（五維度、逐條用 node harness 重現）抓到並修掉的

- **以時間計的週被自己刻意低估的目標打成「超量」**：Phase 1-4 的 Zone 2 跑幾乎每週只有時長沒有
  公里，`timeBased` 這條路幾乎整份計畫都會走到；照表跑滿分鐘、用原文自己的 8-9 分速記公里，
  W1-W8 全部會跳出「已超過本週課表上限」。目標本來就標「約略值、偏低」，不該拿來當硬上限。
  改成 `timeBased` 的週不判斷「超過」。
- **教練設定週目標的上限檢查讀錯資料**：`setWeeklyVolume` 與面板上「課表加總」的文字，讀的是
  操作者自己被 entries 過濾後的加總（標了主動休息／改做會縮小），不是課表本身的加總——
  同一個目標在別人的裝置上卻合法，錯誤訊息報出的數字也是錯的。`weekTargetAuto` 加
  `{planOnly:true}`，教練面板與寫入檢查都改用它。
- **取消「改做」狀態，填過的公里/分鐘留在資料裡繼續被算進週跑量**：畫面上輸入框依項目型態
  決定要不要顯示，取消覆寫回到「照表」後如果這天本來沒有跑步/時長項目，輸入框會消失但數字
  還在——使用者看不到也清不掉。改成回到照表時一併清掉這兩個欄位（僅限當天沒有對應項目時）。
- 目標文字清空後直接靜默刪除整條目標——`app.js` 的邏輯蓋掉了 `store.js` 已經寫好的「清空不
  等於刪除，要刪按 ×」。刪掉那個分支。
- 新增目標的輸入框用 `keydown Enter` 送出，WebKit 中文組字（IME）按 Enter 選字也會被當成送出
  半成品文字——加 `!event.isComposing` 判斷。
- 26 週跑量圖的「課表目標區間」色帶在日間主題幾乎跟卡片背景融在一起——加邊框。

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>

---

## v0.4.2 — 2026-09-11 · 登入後畫面沒顯示已登入（Safari）

Safari（含 iOS）上 `signInWithPopup` 的結果要經過 firebaseapp.com 網域的中繼頁傳回主頁面，
被 ITP 當成跨站儲存擋掉——Google 那邊登入成功、主頁面永遠收不到，也不拋錯。
改成在 Safari 上直接用 `signInWithRedirect`（整頁跳轉）；其他瀏覽器維持 popup，被擋時退回 redirect。
`getRedirectResult` 的錯誤不再被吞掉。

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

---

## v0.4.1 — 2026-09-11 · PlanData 查找表修正 + 教練模式

### 修正：PlanData.userById/videoById/workoutById 永遠是空物件

三個查找表用 shorthand property 回傳，在 IIFE 執行的那一刻（`load()` 還沒跑）就把值
凍結成初始空物件，之後 `load()` 重新指派本地變數不會反映到已經回傳出去的物件上——
跟 `plan/videos/workouts/users` 不一樣，那四個當時就寫成 getter。影響：影片連結、
動作清單、總覽頁的頭像姓名縮寫全部靜默不見，不拋錯，很難發現。改成 getter，跟其他
四個一致。同時加入 Annlin、Phoebe 兩位使用者（`data/users.json` + `firestore.rules.local`
的 `ownerEmail` 對照）。

### 新增：教練模式

三個白名單成員都能切換進一個編輯模式，直接在 UI 上調整共用課表——新增/刪除/調整
順序項目、切換二擇一、編輯週跑量參考、還原成出廠預設值。範圍與取捨見
[決策紀錄第 11 條](docs/決策紀錄.md#11-教練模式課表從共用唯讀改成三人皆可寫)。

**架構**：出廠課表（`data/plan.json`）不變，教練模式的修改存進 Firestore 新集合
`planOverrides/{weekNumber}`，疊加在出廠值上面（`Store.effectiveWeek()`）。每個項目
多了固定 `id`（出廠課表用 `"{週次}-{星期}-{序號}"`，教練新增的項目用
`crypto.randomUUID()`），完成紀錄從「陣列位置對應」（`itemsDone`/`selectedChoice`）
換成「id 對應」（`entries.done`/`selectedItemId`）——這是新增/刪除/排序項目時，
舊的打勾紀錄不會對到錯的項目上的前提。`planVersion`/`schemaVersion` bump 到 3。

### 教練模式程式碼審查：18 個代理、12 條發現通過查證（2 blocker、4 major）

同樣的流程：五維度平行審查 + 逐條查證，實際重現每條發現（不只讀碼），修掉全部：

- **blocker**：`planOverrides` 寫入零 schema 驗證，一份殘缺文件（缺 `days`）會讓
  「總覽」與該週「本週」頁對所有人同時當機——連「還原成出廠預設值」這個自救按鈕
  都畫在會當機的頁面裡，按不到。修法兩層：`Store.saveWeekOverride` 存檔前擋、
  `Store.effectiveWeek` 讀取時也擋（壞資料當作不存在，退回出廠值）；`main.js` 的
  `render()` 也包一層 try/catch 當最後防線。
- **blocker**：教練能直接刪除二擇一日唯一的休息選項，把「可以完全休息」變成
  「一定要做點什麼」，違反決策紀錄第 0 條。`deleteItem` 加兩道防呆：selectOne
  少於 2 個選項擋下、刪除後不再有 `rest` 類型的選項也擋下。
- **major**：教練刪掉使用者已選的選項後，舊的 `selectedItemId` 懸空卻仍被算成
  「完成」，把完成率灌水。`dayStatus`/`weekCompletionRate` 改成先確認選中的 id
  還存在於目前的項目清單裡。
- **major**：整週覆寫（`merge:false`）沒有任何版本比對，兩人幾乎同時編輯同一週時
  後寫的會靜默蓋掉先寫的。改用 Firestore transaction 比對 `baseUpdatedAt`，偵測到
  衝突就中止並提示重新整理，不會無聲覆蓋。
- **major**：時長/距離/RPE 輸入沒有上下限，HTML 的 `min`/`max` 只是裝飾，手滑
  打錯會直接存進所有人共用的課表。存檔前加範圍檢查（時長 0-300 分、距離 0-100K、
  RPE 0-10）。
- 其餘 minor/nit：`toggleDaySelectOne` 開啟二擇一時也檢查至少 2 個選項；還原失敗
  時回滾本機狀態並告知使用者；清空週跑量時補上 `weeklyVolumeNullReason`，避免
  「兩個 null 同時代表不同意思」；決策紀錄補第 11 條記錄這個功能的取捨。

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

---

## v0.3.0 — 2026-09-11 · CP2-CP4 今日視圖／週視圖／總覽／使用者切換／Firestore 同步

一次做完四個頁面 + 同步層。純前端 + Firebase compat SDK，零建置工具，inline
onclick="A.xxx()"，跟 babylog 同一套架構（store/views/app/main 四個模組）。

### 修正：firestore.rules 的 userId ≠ uid

寫程式碼之前才發現：規則寫 `request.auth.uid == userId`，但 `userId` 是
`data/users.json` 的人類可讀字串（"mick"），Firebase Auth 的 uid 是登入產生的隨機
字串，兩者永遠不會相等——照原規則寫入權限全部會被拒。改成 `ownerEmail(userId)`
email 對照表，真實 email 只放 `firestore.rules.local`（gitignore）。

### 新增

- `data/users.json`、`icons/`、`manifest.webmanifest`（PWA，iOS 加入主畫面用）
- `js/plan-data.js` — 唯一允許算日期的地方，`startDate+weekNumber+dayIndex` 推導
- `js/store.js` — 狀態 + localStorage，`Store.activeUserId`（寫入身分）跟總覽頁的
  `viewingUserId`（唯讀查看別人）分開，避免切換查看混淆成切換身分
- `js/firebase-sync.js` — Google 登入 + Firestore 訂閱、離線佇列靠 SDK 的
  `enablePersistence()`，不手刻 outbox
- `js/views.js` / `js/app.js` / `js/main.js` — 四個頁面 + 動作層
- `day.selectOne`：原規格書的「A 或 B」用 optional 欄位表示，等於把休息變成要
  額外選的加購（違反決策紀錄第 0 條）；現在兩個選項地位相等，UI 是選擇題

### CP2-4 程式碼審查：18 個代理、12 條發現通過查證（1 blocker、6 major）

跟 CP1 同一套流程：五維度平行審查 + 逐條查證。修掉全部：

- **blocker**：`setActualStats` 送出 `actualDistanceKm: undefined` 給 Firestore，
  `.set()` 對 undefined 欄位是**同步丟例外**（不是 Promise reject），例外會炸穿整條
  呼叫鏈、連 `.catch()` 都接不到——這筆時長/距離資料完全沒送到雲端，UI 卻毫無提示。

  修法兩層：patch 物件不放沒填的欄位；`pushDoc` 加防禦性清理 + try/catch。
- **選擇題 A→B→A 弄壞已完成紀錄**：`selectChoice` 借用 `toggleItemDone` 的「翻轉」
  語意，在兩個選項間切換會把先選的那個翻回 false，UI 顯示「選了這個」但沒打勾，
  週視圖/總覽把已完成的一天算成未完成。改成每次選擇整組重建 `itemsDone`。
- **選錯身分被誤判成未授權，強制整組登出**：白名單成員（伴侶/教練）切到不是自己的
  `userId` 時，`private/` 因 `isSelf()` 失敗噴 permission-denied，舊版把這跟「真的
  不在白名單」用同一個分支處理，直接 `signOut()`。現在只有 entries/weekAdjustments
  失敗才視為真未授權；private 單獨失敗顯示「身分不符」，不登出。
- **hasPendingWrites 只綁 entries**：只改身體狀況備註或標記本週降量時，同步小提示
  列會誤顯示「已同步」。改成三個集合分開追蹤、OR 合併。
- **未登入期間的本機資料，登入後不會補推上雲**：永久孤兒在該裝置。新增
  `_backfillLocal`，登入時一次性補推，且逐筆比對 `updatedAt` 避免蓋掉遠端新版本。
- **樂觀寫入被拒不會回滾，也沒有逐筆提示**：不回滾（決策紀錄第 0 條：不該因權限
  問題懲罰使用者剛完成的動作），但項目卡片會顯示「尚未同步」標籤。
- 其餘 minor/nit：同步小提示列的錯誤狀態被舊的 pending 蓋掉、`enablePersistence`
  失敗沒有 UI 訊號、`onclick` 裡的 `userId` 沒轉義、安全提醒漏了「每週日自檢」
  那一段、跨專案的教訓註解座標寫錯（指向本專案不存在的行號）。

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>

---

## v0.1.0 — 2026-09-10 · CP0 專案骨架

### 建立

- `git init`（main 分支）、`.gitignore`、first commit
- Port **8772** 登記進 `~/Documents/Projects/CLAUDE.md` 的 Port 註冊表，
  並寫進 `.claude/launch.json` 與 `tools/serve.py`
- 來源文件從 `~/Downloads` 落地到 `docs/計畫原文/`（本機限定，已 gitignore）
- [docs/日曆基準.md](docs/日曆基準.md) — 26 週日期的單一真相來源
- [docs/決策紀錄.md](docs/決策紀錄.md) — 規格審查留下的待決事項定案

### 修正：日期算術

原始計畫與規格書都宣稱 26 週，實際不是。

| | 原本 | 修正後 |
|---|---|---|
| 起算日 | 2026-09-10（**週四**） | 2026-09-07（週一） |
| 總長度 | 179 天 = 25 週又 4 天 | 182 天 = **26 週整** |
| 比賽日 | 構不到第 26 週 | 第 26 週第 7 天 |
| Phase 4 | 19 天（宣稱 3 週） | 21 天 |
| 賽週 | 6 天（宣稱 1 週） | 7 天 |
| schema 第一筆 | `"date": "2026-09-10"` + `"dayOfWeek": "一"`（那天是週四） | 兩個欄位都不存，由 `startDate + weekNumber + dayIndex` 推導 |

原始計畫的階段邊界全部落在週四，但每週課表模板按「一二三四五六日」排——
兩者衝突時**以模板為準**，因為模板是實際要照著練的東西。

### 決定

1. **儲存後端改 Firebase Auth + Firestore**，不用 GitHub Contents API
   （那是 babylog 已廢棄的架構，廢棄理由是 token 對非工程背景的人不友善）
2. **漏尿/疼痛/備註預設完全不分享**，走 `users/{userId}/private/{date}` 子集合
3. **Phase 1 週四改用產後專門課程**，Pamela Reif 從 Phase 2 才開始
   （原計畫第六節自己就說她沒有產後專門系列）
4. **Week 23-26 從第四節進度表推導**，標 `derived: true`，不留空白也不讓 AI 自由發揮
5. **9/07–9/09 標 `expired`**，不計入分母、不提示補做

### 訂下最高原則

> 休息或受傷的調整，不應該變相增加強度。

降量週縮小分母而不是把量往後推；補做只能取代當天內容不能疊加；中斷回歸往後順延而不壓縮剩餘週數。

### 背景

本次改動來自一次多代理規格審查（84 個代理、七維度平行掃描、每條發現逐字回原檔查證
並經對抗式反駁）。原始 46 條發現中 8 條被駁回，去重後 36 條成立：
3 條 blocker、14 條 major、19 條規格缺口。上面的修正與決定涵蓋了全部 3 條 blocker
與其中 9 條 major。
