# 決策紀錄

規格審查（2026-09-10）留下的待決事項，在這裡定案。每一條都寫了「為什麼這樣選」與「反悔的成本」，
之後想改的時候不用重新推導一次。

---

## 0. 最高原則（使用者訂的，覆蓋以下所有細節）

> **休息或受傷的調整，不應該變相增加強度。**

這條不是感想，是可以拿來裁決設計的規則。它直接推翻三種常見做法：

| 常見做法 | 為什麼違反 | 本專案怎麼做 |
|---|---|---|
| 這週降量了，把少掉的量加到下週 | 把休息的代價往後推，等於沒休息 | 降量週**縮小分母**，少掉的量就是少掉了，不補 |
| 漏掉的課排進「補做清單」，累積起來 | 補做疊在既有課表上＝那天變成雙倍 | 補做只能**取代**當天原本的內容，不能疊加 |
| 中斷兩週回來，把剩下的週數壓縮 | 同樣的總量塞進更少的週＝強度上升 | 中斷就是往後順延，或直接接受計畫縮短 |

**驗收方式**：任何一個「調整」功能寫完之後，問一句「使用者用了這個功能，之後某一天的負荷會不會比原本高？」
會，就是做錯了。

---

## 1. 儲存後端 → **Firebase Auth + Firestore**

規格書原本寫「GitHub Contents API」，但那是 babylog **已經廢棄**的架構
（`~/Documents/Projects/babylog/docs/sync.md:4`：「已經整個換掉」）。廢棄理由（同檔 `:10`）：

> GitHub 版本每支手機都要手動申請、貼上一組 GitHub token 才能同步，對非工程背景的家人不友善。

而這個 App 的分享對象正是伴侶與教練。同一個坑不踩第二次。

**這個決定同時解掉四個問題**：blob sha 衝突、離線佇列、寫入權限強制、身分驗證。
Firestore 一天一筆文件（`users/{userId}/entries/{date}`）天生就是文件級併發，兩台裝置改不同天不會互相覆蓋。

**反悔成本**：低。Firebase 專案可以刪掉重來，資料量小。

---

## 2. 敏感健康資料的可見範圍 → **預設完全不分享**

`flags`（漏尿／疼痛／過度疲勞）與 `note`（自由文字身體狀況）**不進共享路徑**。
總覽頁看別人的完成度時，只讀 `status` 與時長的聚合結果，讀不到 flags。

Firestore 路徑分層：

```
users/{userId}/entries/{date}      ← 打勾 / 狀態 / 實際時長 / 距離 / actualNote（白名單內可讀）
users/{userId}/private/{date}      ← flags / note                            （只有本人可讀）
```

Security Rules 裡 `private/` 子集合綁 `request.auth.uid == userId`，不是靠前端不顯示。
babylog `docs/sync.md:23`：「真正的防線一定是 Security Rules，前端檢查只是體驗優化。」

**為什麼不問你**：分享是隨時可以打開的，收回來很難。預設關閉、之後想開再開。

> 第 13 條（2026-09-11）在 `entries` 加了一個共享的自由文字欄 `actualNote`（「實際做了什麼」），
> 那是使用者明確要的、給教練看的欄位，**不是**這條的例外——`private.note` 的邊界沒動。
> 兩個文字框在 UI 上並排，標題直接寫「誰看得到」，見第 13 條。

---

## 3. Phase 1 的週四 → **改成產後專門課程，不用 Pamela Reif**

原始計畫自己就寫了（`docs/計畫原文/訓練計畫.md` 第六節）：

> **Pamela Reif 本身沒有針對「產後/骨盆底」設計的專門系列**

以及「已確認產後恢復良好、醫生已放行一般運動強度」的階段「(通常 Phase 2 之後)」才適合直接用她的影片。
但 Phase 1 模板卻把 Pamela 排進 Week 1-8 的每個週四——同一份文件的正面衝突，
而且衝突方向朝著**安全性較差**的一邊。

裁決：第六節的安全性說明優先於模板。Phase 1 週四改成產後骨盆底/腹直肌專門課程
（原計畫點名的 Fitness Blender *Postpartum Core and Pelvic Floor Recovery Series*），
依規格書 `:93` 的規則**只留搜尋關鍵字、不生成 URL**。Phase 2 起才換成 Pamela Reif。

**反悔成本**：低，改一個 `videoRef`。但反過來（先用了 Pamela 才發現不該用）成本在身體上。

---

## 4. Week 23-26 的 28 天 → **從第四節進度表推導，並標記為推導值**

原始計畫只有 Phase 1/2/3 三張每週模板；Phase 4 只有三條 bullet，賽週完全沒有每日內容。
但第四節的長跑進度表有這四週的資料（W23 20K、W24 12K、W25 8K、W26 3-5K 喚醒跑）。

裁決：用 Phase 3 的每週結構 + 第四節的量，推導出這 28 天，每一筆標 `"derived": true`，
App 上顯示成「推導值，可調整」。**不留 28 天空白**（那會讓 App 在最重要的四週變成空的），
也**不讓 AI 自由發揮**（標記讓你一眼看出哪些不是原文寫的）。

順帶解掉兩個計畫內部矛盾：
- Phase 4 長跑數列，模板寫「12K → 8K → 5K」但第四節寫「20K / 12K / 8K」——**以第四節為準**，5K 歸還給賽週。
- Phase 3 長跑上限三處寫三個數字（26-30K / 28-30K / 26-28K）——**取 28K**，依計畫自己的定位「安全完賽+良好體驗」。

---

## 5. 起算日改 2026-09-07 之後的前三天 → **標記為已過期，不要求補做**

起算日從 `2026-09-10`（週四）改成 `2026-09-07`（週一），26 週才剛好 182 天整、
且比賽日落在第 26 週最後一天。副作用是今天（9/10）一打開 App 就已經在第 1 週第 4 天，
9/07–9/09 三天成了過去式。

裁決：這三天標 `expired`，**不計入完成率分母，也不提示補做**。

理由直接引第 0 條：要求補做 = 第一週就比原本多三天的量。開場就違反最高原則的設計不能留。

---

## 6. 強度以第二節對照表為準，不是模板的「心率/強度」欄

第一版把三張模板的「心率/強度」欄直接抄成資料，結果出現兩套互相矛盾的裁決：長跑用了
第二節的「長跑 Zone 2-3」覆蓋模板，走跑交替卻照模板抄 Zone 2，無視第二節把
走跑交替列在 Zone 1。最嚴重的是 **Phase 1 的長跑本體就是走跑交替**，卻被標成 Zone 2-3／RPE 5-6。

裁決依據不用我自己發明——原文第八節 `:175` 自己寫了操作順序：

> 4. 跑步日 → 對照第四節長跑進度表確認距離/時間,用第二節心率區間確認強度

所以：**第二節的項目類型對照表是強度的唯一真相來源**，模板那一欄只是粗標籤。

| 項目型態 | 心率（第 30 條起寫 Zone） | RPE | 出處 |
|---|---|---|---|
| 恢復跑（原文另含走跑交替，第 12 條起課表裡沒有走跑） | Zone 1 | 2-3 | 第二節 |
| Zone 2 有氧跑 | Zone 2 | 4-5 | 第二節 |
| 長跑（全跑） | Zone 2-3 | 5-6 | 第二節 |
| 節奏跑「稍快於 Zone 2」 | **Zone 3** | 5-6 | **推導的**（第二節沒有這一檔，取往上一區），標 `intensityDerived` |
| Marathon Pace | Zone 3 | 6-7 | 第二節 |

節奏跑跟 Marathon Pace 換成 Zone 之後同一區，靠 RPE（5-6 vs 6-7）區分，見第 30 條。

Phase 1 的長跑：W1-6 現在是 Zone 2 跑（**第 12 條**改的，原本是走跑交替 Zone 1）；
W7-8 第四節寫「40分鐘 全跑」→ 才進 Zone 2-3。
比賽日第二節沒有對應檔位，沿用「長跑」Zone 2-3，並在 notes 寫明。

`verify_plan.py` 綁住這件事的斷言：「強度符合第二節對照表」「長跑強度：W1-6 Zone 2、W7 起 Zone 2-3」
「Phase 1 的 run 一律 Zone 2」——後兩條是第 12 條時取代「走跑交替一律 Zone 1」的。

## 7. 「A 或 B」是選擇題，不是「必做 + 可選的休息」

第一版把原文的「輕鬆跑 30分鐘 **或** 完全休息」編成「跑步（必做）+ 休息（optional）」，
等於把休息變成要額外勾選的加購，預設是做。**這直接違反第 0 條**——把負荷設成預設值。

改成 `day.selectOne = true`：兩個選項地位相等，UI 顯示成選擇題，完成任一即算完成。
全課表共 32 個二擇一日。`optional` 欄位整個移除，verify 有一條斷言擋它長回來。

## 8. 週跑量是參考上限，不是本週課表的加總（已被第 13 條取代）

第四節的「週跑量參考」欄被第一版升級成每週一個權威數字，但同一份 JSON 的 days 湊不到——
Phase 2 只有兩個跑步日，用原文自己的 8-9 分速換算，W13 是 15-17K 而欄位寫 22-25K。

**不自行補跑步日**（那會違反第 0 條：轉檔不該替使用者加量），改成標明性質：
`weeklyVolumeKm.kind = "reference"`，UI 顯示成「參考上限」。沒有數字的週寫明是哪一種
原文狀態（`weeklyVolumeNullReason`），不要讓 null 同時代表三件事。

> **2026-09-11 起作廢**：原文的參考欄整個不再進資料檔。使用者要「每週跑量目標 vs 實際」，
> 而一個照表練也達不到的參考上限拿來當目標，就是用數字催她加跑。現在目標由課表項目
> 即時加總，見第 13 條。`weeklyVolumeKm` 這個欄位名保留給教練模式的手動覆寫（只能往下調）。

## 9. Phase 3 的「重量訓練」不指定 A/B

原文 `:72` 只寫「重量訓練(維持,強度不加量)」。第一版逕自解成 A，結果訓練 B 在最後 10 週
整份消失。改成 selectOne（A 或 B，建議輪替），兩個都標 `derived`——因為是原文沒指定的。

同理，自編的動作清單（`pelvic-core-basic` 的組數次數、`pelvic-core-advanced` 整份）
都標 `derived: true` 並寫明哪裡是推導的。

## 10. 安全性文字用雜湊鎖住

原計畫的醫生放行前提（`:6`）、免責聲明（`:180`）、每週日自檢（`:176`）、
「寧可心率壓低、時間拉長」（`:28`）第一版整段沒搬進來。原文是 gitignore 的本機單一副本，
一旦 CP1 簽掉就在版控邊界之外，不會有人回頭發現。

現在四段逐字進 `plan.json` 的 `safety` 物件，而 `verify_plan.py` 存的是**它們的 SHA256**，
刻意不從 `build_plan.py` import——那樣等於拿產生器檢查自己。改一個標點就會失敗。
本機還有原文時，再額外做一次真正的來源比對。

## 11. 教練模式：課表從「共用唯讀」改成「三人皆可寫」

原始規格書 `:23`「### 3.1 課表資料(共用、唯讀,所有人看到同一份)」與 `:16-17`
「不需要即時多人協作...各自打開自己的紀錄即可」，把課表定為所有人共用的一份唯讀資料。

使用者要求能直接在 UI 上調整訓練課程，不想每次都改 `tools/build_plan.py` 重新部署。
裁決：新增「教練模式」——mick/Annlin/Phoebe **三人都能切換進去編輯**，範圍「完全開放」
（新增/刪除/調整順序項目、切換二擇一、編輯週跑量），不限定某一人是「教練」角色。
這是對規格書 `:23` 的正面覆寫，不是遺漏：三人都是家人/訓練夥伴，沒有敵對關係，
沒有理由把編輯權限限定在一人身上。

**架構**：出廠課表（`data/plan.json`，git 版控）維持不變，教練模式的修改存進 Firestore
的 `planOverrides/{weekNumber}` 集合，疊加在出廠值上面（`Store.effectiveWeek()`）。
沒被改過的週繼續讀出廠值，改過的週優先讀 Firestore 那份。每週都能「還原成出廠預設值」。

**完成紀錄改成用項目 id 對應，不是陣列位置**——這是這個功能能安全存在的前提。
出廠課表的每個項目在 `build_plan.py` 的 `build()` 迴圈裡被賦予 `"{週次}-{星期}-{序號}"`
固定 id；教練新增的項目用 `crypto.randomUUID()`（前綴 `c-`）。`entries.done` 從
`itemsDone`（陣列，位置對應）換成 `{itemId: true}`（map，id 對應），`selectedChoice`
（索引）換成 `selectedItemId`（id）。如果沒有這個改動，教練調整順序或新增/刪除項目時，
舊的打勾紀錄會悄悄對到錯的項目上。

**這個決定引入的風險，跟已經補上的防線**（多代理審查抓到的，見 CHANGELOG v0.4.1）：

| 風險 | 防線 |
|---|---|
| 殘缺的覆寫文件讓所有人的畫面當機 | `Store._isValidWeekShape()` 存檔前擋、讀取時也擋，壞資料當作不存在退回出廠值 |
| 教練能刪掉二擇一日唯一的休息選項，變相強迫使用者失去休息（違反第 0 條） | `deleteItem` 擋下「刪除後不再有 rest 選項」與「selectOne 少於 2 個選項」 |
| 兩人幾乎同時編輯同一週，其中一人的修改靜默消失 | `pushPlanOverride` 用 Firestore transaction 比對版本，偵測到衝突就中止並提示重新整理 |
| 數值輸入沒有上下限，手滑打錯存進所有人共用的課表 | `saveItemEdit` 存檔前檢查時長/距離/RPE 的合理範圍 |

**反悔成本**：中等。拿掉教練模式＝拿掉 UI 入口跟 Firestore 規則的 `planOverrides` 段落，
`Store.effectiveWeek` 退化成永遠讀出廠值——出廠課表本身不受影響。已經寫進去的
`planOverrides` 文件可以留著不管（沒有 UI 讀取時就是死資料）或手動清空。

## 12. 課表裡沒有「走跑」：Phase 1 跑步日改 Zone 2 跑（強度 Zone 1 → Zone 2，使用者裁定）

使用者裁定（2026-09-11）：「不要有走跑的項目，應該是 Zone 2 跑，走跑太含糊。」

原文 Phase 1 的週二／週五跑步日與 W1-6 的長跑都是「走跑交替」（第二節:32，Zone 1）。
現在整個拿掉：`walk-run` 這個類型從 `build_plan.py`、`verify_plan.py`、`views.js` 的
`VALID_TYPES` 全部移除，Phase 1 的跑步日改成 `run`「Zone 2 跑」，**時間數字完全不變**。

**這是負荷上升，要攤開講。** 第 1-8 週的週二／週五加第 1-6 週的週六，共 21 個未過期的日子，
心率從 Zone 1 變 Zone 2、RPE 從 2-3 變 4-5。第 0 條的驗收句（「之後某一天的負荷會不會比原本高？」）
對這次改動的答案是「會」——但第 0 條管的是**休息或受傷的調整**不能變相加量，這次是使用者
對自己課表的正面裁定，且她用的字眼就是「Zone 2」（第二節明確的那一檔）。
記在這裡的用意是：這是她知情的決定，不是轉檔順手帶過的加量。Phase 1 跑步項目的 notes
補上原文第 28 行的「Zone 2 上限先抓保守一點」與講話測試（她沒有最大心率數據）。

**W1-6 的長跑用 Zone 2，不是 Zone 2-3。** 它現在的 type 是 `long-run`，第二節「長跑」的
慣用強度是 Zone 2-3——但原本是 Zone 1 的走跑，改成 Zone 2 跑只該進一級，順手標成長跑強度
等於連跳兩級。W7-8 原文寫「40分鐘 全跑」才進 Zone 2-3。`verify_plan.py` 的
「長跑強度：W1-6 Zone 2、W7 起 Zone 2-3」與「Phase 1 的 run 一律 Zone 2」兩條斷言綁住這條，
第 6 條的「走跑交替一律 Zone 1」斷言隨走跑一起移除（第 6 條本文已同步改）。

**已存在的教練覆寫**：教練模式在這之前改過的週，覆寫文件裡可能還有 `walk-run` 項目。
它們照常顯示（標「走跑交替（舊類型，請改選）」），算跑量時視同 `run`；教練點編輯時必須改選
類型才能存檔，不會被靜默換成別的類型。覆寫文件從此記 `basePlanVersion`，低於出廠版本時
教練面板顯示「這週的調整基於舊版」並指向「還原本週」。

**反悔成本**：低。改回走跑是 `build_plan.py` 的模板改回去、`VALID_TYPES` 加回一個字串、
重跑產生器。已經在 Firestore 的完成紀錄用項目 id 對應，id 沒變，舊紀錄不受影響。

## 13. 舊 Notion 課表格式的整合：每日「實際操作」、四態狀態、週跑量目標 vs 實際、訓練目標

使用者的前一份訓練計畫（Notion，三鐵）用了 15 週的表格，穩定的核心是四欄：
星期｜訓練內容｜重點／強度｜**實際操作（完成度）**。她實際在最後一欄寫的是四種東西混在一起：
狀態（完成／錯過／休息）、替代（「改肌力訓練」「受傷 改 單車」）、實際數字、身體感受。
這條把那個格式拆進現在的資料模型，並加上使用者另外要的「每週跑量目標與實際」和「可以設定訓練目標」。

### 13a. 每天一個「實際操作」方塊（`entries.actualNote`、實際分鐘／公里搬到這裡）

- `actualNote`：自由文字，跟打勾同一份文件、白名單三人可讀（Notion 那欄本來就是給教練看的）。
  身體狀況仍寫 `private.note`（第 2 條不動）。兩個文字框並排、**標題直接寫誰看得到**
  （「Annlin、Phoebe 看得到」／「🔒 只有你看得到」），私密欄從收合改成預設展開——
  提示文字不是防線，兩個框都看得到才會分流。
- 實際分鐘／公里本來就是一天一筆（`entries.actualDurationMinutes / actualDistanceKm`），
  之前畫在每張項目卡上，一天兩個有時長的項目就是兩個輸入框綁同一個值。現在只畫在這個方塊。
  公里欄在當天有跑步類項目、或狀態是「改做」時顯示（改做成跑步也要能記公里）；
  「錯過」「主動休息」時不顯示、且數字清成 null。

### 13b. 一天的狀態：打勾推導 + 三個覆寫值（`entries.status`）

`status` 只有 `null`（照表，由打勾推導 done/partial/pending）、`substituted`（改做）、
`missed`（錯過）、`rested`（主動休息）四種。**沒有 `done`**——完成永遠靠打勾，不另存一個
會跟打勾矛盾的欄位。打勾跟覆寫互斥、由 `Store` 維護：打勾＝清掉 status；設覆寫＝清掉勾。
「照表」不是一顆可以按的 chip，是方塊右上角的唯讀文字（「照表 · 部分完成」）。

完成率（「照表完成率」）的分母：拿掉過期日、課表本身全休息的日子、`rested`、**以及 `substituted`**。
改做是第 0 條唯一允許的補做方式（「取代，不疊加」），把它算成跟錯過同分會把人推向硬照表操課。
`missed` 留在分母、不進分子。不另算「有練率」——多一個分數就多一個催人的數字。
純休息日不放狀態 chip（在休息日給「改做」按鈕＝App 主動遞出「用訓練取代休息」）；
未來的日子只能預先排「主動休息」。

### 13c. 週跑量目標 vs 實際（取代第 8 條）

目標**不存進 `plan.json`**，由 `Store.weekTargetAuto` 從該週的跑步項目即時加總——教練改了
項目，目標自動跟著變，不會有兩個必須互相對應的數字。規則：

- 只算 `run / long-run / tempo`（＋舊的 `walk-run`）；`race` 不算——比賽是終點不是那週的跑量，
  算進去賽週目標 47K+，26 週的圖被那一根壓扁。賽週卡片另外一行「週日比賽 42.195 km 另計」。
- 只有時長的項目（Phase 1 全部）用 `plan.timeBasedRunPaceMinPerKm` 換算，**取原文 8-9 分速的慢端 9**：
  公里＝分鐘÷分速，分速數字越小目標越高，要低估就取慢端（第一版寫 8 還註解成「偏低」，方向反了，
  W7-8 算出 10-11.9K 超過原文自己的 8-10K 參考上限）。`verify_plan.py` 守 `≥ 9`。UI 標「約」。
- 過期日、使用者標 `rested` 的日子、`substituted` 且沒記公里的日子（改騎車、改核心）不算——跟完成率的分母
  同一套（第 0 條：降量週縮小分母）。`substituted` 有記公里（改做的也是跑步）就照算，跟實際對得上。
  `missed` 照算，缺口是真的。
- 二擇一的日子：下限取各選項最小值（休息＝0，所以含休息的日子下限自然不含它）、上限取最大值。
- 實際＝各天公里加總；照表的日子沒填公里但有填分鐘、且當天有跑步項目 → 用同一個分速換算
  （跟目標對稱，否則 Phase 1 八週的實際永遠是「—」），標「約」。改做的分鐘不換算（不知道是不是跑步）。
- 已標記「本週降量」的週：不畫進度條、不比對，只顯示實際（原定目標放在括號裡）。
- 進度條的 100% 點是目標**下限**（碰到下限就滿格）；超過上限改顯示警示文字「已超過本週課表上限——下週不要再加」，
  不畫「多出來」。填滿型的條會催人往上限跑。

**教練模式可以手動設定週目標**（使用者要求「可以設定訓練目標」），但**上限不能高於課表加總**——
數字只能把目標調低。要加量請改課表項目，那才看得見、也才會被審。留空＝改回自動加總。

### 13d. 訓練目標（`users/{userId}/profile/goals`，每人各自）

比賽目標一句＋自訂目標清單（文字＋達成勾）。白名單可讀（一起練的人看得到）、只有本人可寫。
**目標只收文字、不畫進度、不跟週跑量卡連動**——否則它會變成使用者自己給自己加的第二支進度條。
Notion 的「總目標」（VO₂max 34 → 40）與「檢查點任務」都可以寫成一條條文字。

### 13e. 週視圖加「表格」模式

三欄「日｜課表｜實際」並排，模仿 Notion 表格給回顧用；卡片模式維持原樣給手機打勾用。
切換用本機 localStorage 記住（跟主題、教練模式同一類裝置偏好）。

### 順帶修掉的既有問題

- `firebase-sync.js` 綁 `Store._cloudPushPlanOverride` 時少接了第三個參數 `baseUpdatedAt`，
  版本比對永遠拿到 `undefined`，同一週第二次教練編輯必定被判成「剛被別人改過」而丟掉；
  `_cloudDeletePlanOverride` 同樣少接 `backup`，還原失敗永遠不回滾。改用 `bind`，參數數量不可能再對不上。
- 遠端文件寫回本機從「整份 last-write-wins」改成「逐欄位合併」（`mergeDocs`）：雲端是 `set(merge:true)`
  逐欄位合併，但 `updatedAt` 整份一個——兩台裝置分別改同一天的不同欄位時，舊做法兩台各只看到自己的欄位，
  且永遠不會自我修正。
- 總覽頁在未登入時就訂閱別人的 entries，訂閱失敗後仍被記成「已訂閱」，登入後不再重訂——現在未登入不訂閱。
- `_writeOwnEntry` 對 `undefined` 欄位直接丟例外（可執行守衛，不是註解）：`undefined` 會讓本機清掉欄位、
  雲端卻保留。

**反悔成本**：低到中。狀態與 actualNote 是純加法欄位，舊文件缺 key 讀成 null；拿掉功能只是拿掉 UI，
資料留著無害。週跑量改回存數字則要回到第 8 條的兩個真相來源問題，不建議。

## 14. 環境因素讓某天跟另一天對調：個人、單週的「顯示順序」，不是課表內容改變

使用者的情境：這禮拜因為環境因素，週一跟週三的內容要對調（例如場地/天氣讓她今天只能做
原本排週三的內容）。討論過三個方向（整天對調／單一項目搬到別天／拿掉「哪一天」這個
限制改成本週待辦清單），選了**整天對調**：

- 它精準對應「對調」這個情境，其餘兩個方向要嘛沒對到（清單模式）、要嘛有風險（單一項目
  搬到別天可能讓某天疊兩份課，是第 0 條要防的「補做疊在既有課表上」）。
- 它是一個**排列**（permutation）：七天的內容一個不多一個不少，第 0 條的驗收句
  「會不會變相加量」直接答「不會」——這是唯一不需要額外防呆規則就通過的方案。

**資料模型**：`weekAdjustments/{weekNumber}`（跟「本週已降量」同一份文件、同一套可見性——
白名單可讀，只有本人可寫）多一個欄位 `dayOrder`：長度 7、涵蓋 0-6 的排列，
`dayOrder[i]` = 日曆上第 i 天（週一=0）實際顯示出廠課表第幾天的內容。預設（欄位不存在或
不合法）當作 identity `[0,1,2,3,4,5,6]`。**課表項目本身完全不動**，只是換一個標籤讀。

**日期歸屬不變、內容歸屬換**：`entries` 仍然用日曆日期 + 項目 id 對應（沒有新欄位）——
「哪天做的」看日期，「做了什麼」看項目 id，兩者本來就是分開存的，對調只是改變「這個日曆
格子該顯示哪個出廠天的項目清單」，完成紀錄的儲存方式完全不用變。這是當初教練模式把
項目 id 做成固定值時順便換來的（決策紀錄第 11 條）。

**教練模式一律忽略對調**：`Store.effectiveDayOrder` 在 `coachMode` 開著時永遠回傳 identity。
理由：教練模式編輯的是所有人共用的課表結構，且畫面上「日曆格子」跟「要編輯的出廠天」
是同一個 dayIndex（`renderDayDetail` 的教練工具列、`_cloneEffectiveWeek` 都是這樣用的）——
如果對調在教練模式下也生效，會編輯到錯的一天。這也代表個人對調的 UI（週視圖的「本週
順序」卡片）在教練模式開著時不顯示，因為顯示了也不會生效，只會讓人以為壞了。

**跨人看進度也要拿得到對方的對調**：總覽頁「查看別人的進度」算完成率／週跑量時，
如果不知道對方自己的 `dayOrder`，會拿錯的內容去對他的打勾紀錄（他打勾當下看到的是
他自己對調後的畫面）。所以 `weekAdjustments` 的 Store 快取從「只存自己」改成
`{ [userId]: {...} }`（跟 `entries` 同一個形狀），並新增 `Sync.subscribeOtherWeekAdjustments`，
跟既有的 `subscribeOtherEntries`／`subscribeOtherProfile` 同一套。

**反悔成本**：低。拿掉功能＝ `effectiveDayOrder` 永遠回傳 identity，UI 拿掉「本週順序」卡片；
已經存在的 `dayOrder` 欄位是死資料，不影響任何計算。

## 15. 教練模式下可以互相設定訓練目標，不限定某一人是教練

使用者（mick）提出：「我是教練，我要給 Annlin 跟 Phoebe 設目標」。

裁決：**沿用第 11 條教練模式已經定案的哲學**——「三人都是家人/訓練夥伴，沒有敵對關係，
沒有理由把編輯權限限定在一人身上」。所以不是「只有 mick 能設別人的目標」，而是
**教練模式開著時，白名單內任何人都能編輯任何人的訓練目標**，跟教練模式改課表內容
用同一顆開關、同一套精神。程式碼裡完全不出現 mick 這個特定身分——避免把一次性的
使用情境寫死成程式邏輯，之後角色互換（例如 Phoebe 也想幫 mick 設配速目標）不用改碼。

**規則層**：`firestore.rules` 的 `users/{userId}/profile/{doc}` 寫入權限從
`isSelf(userId)` 放寬成 `isMember()`——跟 `planOverrides` 同一個模式（規則層不知道
「教練模式」這個 UI 開關是不是開著，那是體驗層的守門，不是安全邊界；真正擋住的是
白名單本身）。這個集合目前只放 `profile/goals`，不放身體狀況等敏感資料——那些走
`private/` 子集合，權限沒有變。

**程式層**：`Store` 的目標寫入函式（`setRaceGoal`/`addGoal`/`toggleGoalDone`/`setGoalText`/
`removeGoal`）都加一個可選的 `targetUserId`，不傳＝寫自己的（維持原本行為，向後相容）。
教練幫別人寫的目標**只留在記憶體**（靠既有的 `subscribeOtherProfile` 保持最新），
不寫進教練自己裝置的 localStorage 快取——那是別人的資料，不該用自己的裝置快取它。
`renderGoalsCard` 的可編輯條件從「只有自己」改成「自己，或教練模式開著」；後者顯示
「教練模式編輯」標籤，避免看起來像目標是自己的。

**目標本身的邊界沒有變**（決策紀錄原文第 13d 條）：只收文字、不畫進度、不跟週跑量卡
連動，也不會改變任何一天的課表內容——這條裁決只放寬「誰能寫」，不放寬「目標能做什麼」。

**反悔成本**：低。改回「只有本人能寫」＝ rules 的 `profile` 寫入改回 `isSelf(userId)`、
`renderGoalsCard` 的 `editable` 條件拿掉 `|| Store.coachMode`。已經被教練寫入的目標
資料本身沒有格式差異，不需要遷移。

## 16. 登入流程以 babylog 為準，不做任何瀏覽器／裝置判斷

登入問題回報了三次（v0.4.2、v0.6.1、v0.6.2 各修一次），三次修法全部是錯的方向，
最後一次（v0.6.2）更是把原本能用的「加入主畫面」模式整個擋掉：

| 版本 | 假設 | 實際 |
|---|---|---|
| v0.4.2 | Safari 的 ITP 擋掉 popup 的回傳 → Safari 改用 `signInWithRedirect` | 方向相反。Firebase 自己的文件寫明：在會擋第三方資料的瀏覽器上，redirect 導回來會拿到空使用者，**建議的做法是用 popup** |
| v0.6.1 | UA 判斷漏掉 iOS 上的 Chrome/Firefox（也是 WebKit）→ iOS/Firefox 一律 redirect | 把 v0.4.2 的錯誤方向擴大到更多瀏覽器，Safari 分頁也登不進去了 |
| v0.6.2 | 「加入主畫面」是嵌入式 webview，Google 封鎖、儲存空間隔離 → 直接不讓登入 | **錯**。babylog 與日文學習 App 同一套架構（GitHub Pages + firebaseapp.com authDomain + compat SDK 12.17.0）在主畫面模式登入、同步都正常。網路搜尋到的「webview 被封鎖」講的是原生 App 內嵌的 WKWebView，不是 Safari 的主畫面網頁應用程式 |

裁決：**登入流程一字不差照抄 babylog 的 `signInWithGoogle()`**——`signInWithPopup` 優先，
只有 `auth/popup-blocked` 才退回 `signInWithRedirect`，`auth/popup-closed-by-user` 回 idle，
其餘顯示失敗。不做瀏覽器 UA 判斷、不做 `navigator.standalone` 判斷、不做逾時計時。
順便把 babylog 2026-08-02 事故的 `experimentalAutoDetectLongPolling` 也帶進來（同一支手機、
同樣的行動網路，串流連線被中間設備弄斷時自動退回 long-polling）。

兩個保留的、跟 babylog 不同的小改動（都只是讓失敗看得見，不改變登入機制）：
- `renderSyncPill()` 在「沒登入」的區塊也判斷 `signing-in`／`fail`／`unauthorized`——
  之前這些狀態排在「沒登入 → 點擊登入以同步」之後，永遠顯示不出來。
- `_handleSnapErr` 判定未授權後 `signOut()`，`onAuthStateChanged` 的 null 分支**保留**
  `unauthorized` 狀態並在訊息裡寫出被拒絕的 email（babylog 也這樣做）——不然畫面會退回
  「點擊登入以同步」，使用者看到的是「登入完全沒發生」，而不是真正的原因（帳號不在
  白名單／`firestore.rules.local` 改了沒發布）。

**這次的教訓（記在 memory，供所有專案）**：手上有同一套架構、確定能用的先例
（babylog）時，**先逐行比對先例，不要用網路搜尋的結果推翻先例**。三次修法都是「搜尋
→ 找到一個聽起來合理的成因 → 改」，沒有一次先去看 babylog 到底怎麼寫的；而 babylog
的程式碼從第一天就擺在那裡。搜尋結果描述的是「某些情境下會發生的事」，先例證明的是
「這個情境下不會發生」，後者的證據強度高得多。

**反悔成本**：無——這是回到已經驗證過的做法。

## 17. 拿掉「今日」頁：本週頁改手風琴；新增「改做類型」與「體感強度」兩個紀錄欄位

使用者要求（2026-09-12）：刪掉今日頁，想保留的內容搬到本週頁、改成手風琴展開；
實際操作加「改做了什麼」的快速選項；新增 Apple Fitness「Rate Your Effort」式的體感強度；
身體狀況收合；安全提醒收進「！」；跑量說明收進「？」；底部被導覽列遮住要修。

**手風琴**：本週頁七列各自可展開，一次只開一列，展開的列身就是原本今日頁的內容
（訓練說明 → 實際操作 → 身體狀況）。開 App、按「本週」、切到今天那一週時，**今天那一列
自動展開並捲到可見**——「打開就看到今天」這件事保留，只是不再是獨立一頁。導覽列變三格。
表格模式（課表｜實際）留著當回顧用。

**改做類型（`entries.substituteType`）**：點了「改做」才出現第二排——跑步｜重訓｜核心｜
騎車｜游泳｜走路｜其他。兩下點完，不用打字。週跑量規則跟著變準：**只有改做「跑步」的
公里才算**（之前是「改做＋有填公里就算」，改騎車填 20 km 也會被加進跑量，不對）；
改成非跑步類時公里欄不顯示、已填的公里清掉。沒選類型的舊紀錄維持舊規則。

**體感強度（`entries.effort`，1-10）**：這是「實際」，跟課表項目的 `rpe`（「要求」）分開存。
UI 照 Apple 的四階梯：1-3 輕鬆、4-6 中等、7-8 困難、9-10 全力，十個點點哪個就是幾分，
再點一次清掉。課表要求的 RPE 區間在階梯上用淡色點標出（二擇一取選中的那個項目）。
**只有「打得比課表高」才提示**（「比課表要求吃力——隔天看狀況」）——第 0 條：實際比要求
吃力是要留意的訊號，不是反過來要往上打；打得低什麼都不說。錯過／主動休息的日子清掉、
不顯示（沒東西可評）。

**身體狀況**：三顆旗標常駐一排；備註框有旗標、有內容、或點「＋備註」才展開。「只有你
看得到」標籤常駐——分流靠標題，不靠 placeholder（第 13a 條的原則不變）。

**安全提醒**：本週頁標題旁一顆「！」，點了開全文彈窗。第 10 條的四段文字**可以藏，
不能少**——`verify_plan.py` 只驗 `plan.json`，畫面這邊靠 `renderModal` 自己守四段都在。

**底部留白**：`#root` 的 `padding-bottom` 從寫死 84px 改成 `calc(84px + env(safe-area-inset-bottom))`
——主畫面模式下 iPhone 的 home indicator 安全區會完整算進導覽列高度，之前差十幾到二十幾 px。

**還沒做（第二批）**：拖曳換順序取代「本週順序」卡片（要接觸控，用 SortableJS）；週日回顧
統計「這週幾天比課表吃力」。

**反悔成本**：低。兩個新欄位是純加法（舊文件缺 key 讀成 null）；手風琴是純顯示層，
`renderDayBody` 就是原本的 `renderDayDetail` 去掉標頭。

## 22. 階段性目標：Zone 2 配速、跑量、5K 技術指標，依訓練階段各自設定

使用者問「教練部分要怎麼設定不同時期的目標」（2026-09-13）。查證後發現兩件事沒對上：
`data/plan.json` 每個階段本來就有一句目標說明（例如恢復奠基期「核心／骨盆底重建…」），
但畫面上從沒顯示過；現有「訓練目標」卡是整個 26 週共用一份，沒有分階段的概念。
討論後確認要新增的是**結構化的數字目標**，不是那句既有的文字說明：

- Zone 2 配速目標（分鐘/公里）
- 跑量目標（公里）——有「本階段累積實際」比對，沿用週跑量的算法逐週加總
- 5K 技術指標目標：Cadence 步頻、Vertical Oscillation 垂直振幅、Ground Contact Time
  觸地時間、Stride Length 步幅（跑錶 Running Dynamics 的四個常見欄位）

**跟第 0 條的關係，刻意跟週跑量目標不同**：週跑量目標是系統自動算出來、教練只能往下調
的天花板（第 13 條）——那是每週都在調的高頻動作，容易手滑，需要機械防線。這裡的六個
數字改成**不設自動上限、不擋存檔**，理由是使用者的判斷：「這應該是一個目標數字，不用
連動或限制，這樣學員可以看到累積跑量跟目標跑量的差異」——這是教練低頻率、深思熟慮的
專業判斷，不是系統自動把負荷往上推，不適用同一套機械擋法。App 只在旁邊顯示背景參考，
**不擋、只提醒**：
- 跑量目標旁邊顯示「課表這階段大約 X–Y km」（`phaseVolumeAutoRange`：該階段每週自動
  目標加總，跟 `setWeeklyVolume` 用的天花板同一個算法）；教練填的上限超過這個參考值時
  多一句「你的目標比這高」，純文字提醒，不阻止存檔。
- Zone 2 配速目標旁邊顯示這階段 Zone 2 的體感（第 30 條起是「RPE Y、強度說明」，原本是「對應心率 X%、RPE Y」）（`findZone2Reference`：
  找該階段第一個標題含「Zone 2」的跑步項目）——配速沒有像跑量那樣現成的自動上限可以
  拿來卡，只能提供這個當背景。

**5K 技術指標跟配速目前只設目標、不比對實際**：使用者確認「先做這版」——量測 5K 實測
數值需要另外設計（哪些紀錄算數、單次測驗還是多次取平均），留到之後有需要再談。

**資料**：`users/{userId}/profile/phaseTargets` 文件，跟 `profile/goals` 同一個集合、
同一條規則（`isMember()` 可寫，不用改 `firestore.rules`）——查了同步程式碼，`profile`
本來就是整個集合訂閱（當初就是為了以後加新文件留的），只需要在 `docChanges` 的分派
邏輯裡加一個 `phaseTargets` 分支。頂層欄位是 phaseId（`p1`／`p2`／`p3`／`p4`／`race-week`），
逐階段做 `applyPatch` 欄位級合併——兩個教練同時改**不同階段**不會互相蓋掉；同一階段
內的六個欄位是一次整份寫入（教練一次填完六項按一顆「儲存」，不是六次個別存檔），
跟項目編輯表單同一種「整份讀、整份存」模式。跟訓練目標同一套權限：本人隨時可編輯，
教練模式下任何白名單成員都能幫別人設（第 15 條）。

**反悔成本**：低。新文件、新集合分支，不動任何既有欄位；不喜歡的話刪掉這張卡片跟
對應的 Store／firebase-sync 分支即可，資料留著不影響其他功能。

## 18. 本週順序改成拖曳；「本週順序」卡片拿掉

第 17 條的第二批（2026-09-12）：「刪除切換順序欄，改成手動拖曳互動式切換」。

**做法**：每列右邊一顆 ⋮⋮ 把手，按住拖到想要的位置放開。**插入語意**（把第 from 格
拿出來插到第 to 格，中間順移一格），跟 iOS 清單拖曳一樣——不是「兩格對調」。
資料層還是第 14 條那個 `dayOrder` 排列，只是入口從兩個下拉選單變成 `Store.moveWeekDay`；
拖回出廠順序時一樣清掉 `dayOrder` 不留空紀錄。`swapWeekDays` 刪了（沒有第二個入口）。

**用 SortableJS（cdnjs）不自己寫**：觸控拖曳要處理長按判定、跟捲頁打架、自動捲動、
放開時的動畫——這些都是別人踩過幾百次的坑。設 `delay:150, delayOnTouchOnly:true`
（手指按住一下才開始拖，短按照常展開那一列）、`forceFallback:true`（桌機手機同一套，
不靠瀏覽器原生 DnD，iOS Safari 的原生 DnD 行為不一致）。**沒載到就退化**：`views.js`
不畫把手、`app.js` 不掛，其他功能不受影響；「還原順序」不靠它——所以離線或 CDN 被擋
只是不能拖，不是壞掉。

**每次重繪重新掛**：`render()` 整個換掉 `#root`，所以 `main.js` 畫完呼叫 `App.afterRender()`
在新 DOM 上重掛。`onEnd` 裡用 `setTimeout(…,0)` 再重繪，不在 Sortable 還握著節點時把
DOM 換掉。展開的那一列跟著內容走（被拖的就是它 → 落點；在拖動範圍內的 → 順移一格）。

**留下的**：一行提示「按住 ⋮⋮ 拖曳可換這週的順序」；對調過之後變成「已對調：週一顯示
週三的內容」＋「還原順序」。表格模式跟教練模式不能拖（教練模式下 `effectiveDayOrder`
一律回出廠順序，第 14 條）。

**順手加的**：週日回顧多一行「這週有 N 天體感比課表要求吃力——下週照表，不要加」
（第 17 條的 `effort` 對項目 `rpe.max`）。只提醒不要加，不會建議補——第 0 條。

**反悔成本**：低。資料格式沒動；把 SortableJS 拿掉就是回到沒有拖曳的畫面。

## 19. 教練編輯表單拿掉「強度是內插值／內容是推導值」兩個勾選；教練存過＝確認過

使用者在手機上看到教練模式的編輯表單最底下兩個勾選框的字整個直排（每個字一行），
問「這兩個 check box 是做什麼用的」（2026-09-12）。

**它們是什麼**：`derived`（內容是推導值）跟 `intensityDerived`（強度是內插值）是
`tools/build_plan.py` 轉檔時留下的**來源標記**，共 60 筆、三種來源：第 4 條的 W23-26 整段
（32 筆，原課表沒有每日內容）、Phase 2 重訓 A／B 的時長（16 筆，原課表有這項、只是 30 分
是沿用 Phase 1）、W17-22 的重訓 A／B 二擇一（12 筆，原文只寫「重量訓練（維持）」沒指定）。
畫面上掛「推導值」標籤。tempo 的心率原課表沒給，當時取 Zone 2 跟馬拉松配速中間內插，掛
「（內插）」（第 30 條起寫成 Zone 3，標籤改叫「（推導）」）。兩個標記**只影響標籤顯示，不進任何計算**（跑量、完成率、RPE 對照都不看它）。

**為什麼拿掉**：它們是「還沒有人確認過」的意思，不是教練要決定的事——教練打開表單
存檔，就是人看過了。留在表單裡只會讓三個非工程背景的人猜「我該勾嗎」。裁決：
表單不再顯示勾選框；**存檔一律清成 false**（新增的項目本來就是 false）。如果被編輯的
項目本來有標記，表單最上面一行講清楚它是什麼、存檔後標題旁的標籤會拿掉——資訊還在，
決定權不在。措辭不說「原課表沒寫」（對 Phase 2 重訓是錯的），說「沒寫清楚的部分是補的，
細節看備註」。直排的版面問題順便消失（沒有那一列了），不用去修 flex 的 min-width。

**二擇一是一組**：兩個選項並排在同一天，存了其中一個就整組清掉 `derived`——不然標籤只剩
在另一個選項上，兩個對等的選擇顯示不對等（`verify_plan.py` B4 對出廠資料守的就是這件事）。
`intensityDerived` 是逐項的心率來源，不跟著清。動作清單自己的「推導值：…」說明（`workouts.json`
的 `derived`）是清單的，不受項目存檔影響。

**順手修的**：
- 同步膠囊「寫入被拒」在手機上點不出原因（原因只放在 hover 的 title），改成點了直接顯示。
- `profile`（訓練目標）被拒時的訊息改講真正的原因：Firebase 上的規則可能還是舊版——之前那句
  「請確認登入的帳號跟裝置上選的身分一致」在「教練幫別人設目標」這個情境下無從照做。
  她截圖裡那顆「寫入被拒」最可能就是 v0.6.0 的 `profile` 規則還沒重新貼進 Firebase Console。
- `write-denied` 狀態會被 entries 的快照洗回「已同步」（被拒的寫入回滾本身就再觸發一次快照），
  排除名單補上它；等被拒的那些 key 重寫成功才回到 done。

**反悔成本**：低。要把勾選框加回來是兩行 HTML＋兩行讀值。

## 20. Firestore 規則的 userId → email 對照：從連鎖 `?:` 改成純 `&&`／`||`（照 babylog）

使用者把 `firestore.rules.local` 貼進 Firebase Console，編輯器在 `ownerEmail()` 第三層
`?:` 那一行標了紅點（2026-09-13）。本機沒有 Java 跑不了 emulator、CLI 沒登入，
無法在本機驗證規則語法。

裁決：不去猜規則語言支不支援巢狀三元——**照先例**（第 16 條的教訓：登入／同步／規則這類
事一律先比對 babylog）。babylog 的 `isXxxFamily()` 全是純 `&&`／`||` 的布林函式，已經在線上
跑很久；把 `ownerEmail()` + `isSelf()` 合成一個同樣寫法的 `isSelf()`：
`(userId == 'mick' && authEmail() == '…') || (…)`。語意完全一樣，加人＝加一行 `||`。

**反悔成本**：無——這只是換寫法。若貼上後紅點還在，原因就不是這個，要看編輯器的訊息。

## 23. 當天紀錄卡重排：課表併進來、數字排第一、填數字自動完成、拿掉「錯過」

使用者兩輪意見（2026-09-13）：第一輪要拆掉「每天展開後三張卡各自為政」的問題，第二輪
給了具體排序：課表項目當標題，底下照「運動回來先記什麼」排——**實際公里／分鐘排第一**，
其次是「沒照表？更換項目／自主休息」，再來體感強度、附註、身體狀況。按鈕文案：
改做→更換項目、主動休息→自主休息，「錯過」拿掉（「跟改做、主動休息都沾到邊，存在意義
不大」）。先出了兩版視覺對照稿讓她確認，再照對照稿裡查出來的四個決定動手：

1. **填了公里或分鐘就算完成**——但只在「非二擇一、當天只有一個不是休息的項目」時
   （多項目的日子一個數字分不出是哪一項做完，照舊逐項打勾；二擇一「選」本身就算完成，
   第 13b 條）。`Store.setActualStats` 在這個條件下順手把 `done[thatId]` 設成
   `true`——**只單向打勾**：清掉數字不會取消完成，要取消得手動點「完成」，避免手滑
   清空數字變相把已經做完的一天打回沒做（第 0 條的精神：調整不能讓她「損失」已經
   記錄的東西）。
2. **過去沒記錄的日子顯示「未完成」，不是「待完成」**——「待完成」暗示還欠著、該補，
   對已經過去的日子讀起來像催促（第 0 條）；今天和未來的日子維持「待完成」，因為那還
   在她的掌控內。`dayStatusLabel(status, dateKey)` 統一處理，週列表、表格模式、紀錄卡
   標題都走同一個函式，不是各自判斷一次。
3. **課表二選一、其中一個是完全休息的日子，不放「自主休息」按鈕**——課表本身已經是
   休息選項，兩種休息只留一條路（沿用第 13b 條原有的規則，這次擴大到合併後的卡片）。
4. **「＋備註」改「＋寫給自己」**：她的判斷是「以紀錄的角度不太可能這麼常打字」，
   先這樣，以後看使用者回饋再調——不是現在就要解決附註／身體狀況兩個文字框「長得太像」
   的問題，是记录下来先不當一回事。

**拿掉「錯過」，數字不變**：`missed` 在完成率、週跑量目標裡的算法本來就跟「什麼都
沒按」一樣（`weekCompletionRate`／`weekTargetAuto` 都只看 `rested`／`substituted`，
從沒看過 `missed`）。真正要處理的只有畫面文字（見上面第 2 點）跟**舊資料相容**：
`DAY_STATUS_OVERRIDES` 拿掉 `'missed'`，新增 `entryStatus(e)` 當唯一的讀取入口——
舊文件裡存著的 `status:'missed'` 一律讀成 `null`（照表），**不改資料**，也不會讓
「填數字自動完成」漏掉這些舊紀錄（`setActualStats` 一併把殘留的舊 status 清掉）。
規則：**任何地方要看一天的狀態一律呼叫 `entryStatus()`，不直接比對 `e.status`**——
這是唯一一處，以後 v3 那種「舊值卡在資料裡、程式碼卻假設不存在」的教訓才不會重演。

**一張卡到底**：`renderActualBox`／`renderFlagsBox`／`renderWeeklyReviewCard`（原本
只在週日出現）拆掉，合併進 `renderDayRecordCard`（見上一輪 v0.7 系列的「三張卡看起來
不像同一次紀錄」診斷）——課表項目的說明（時長／心率／RPE／影片／動作清單）抽成
`itemPlanParts()`，教練模式的項目卡跟一般模式的紀錄卡共用同一份，改一處兩邊一起變。
子區塊用 `.rec-sec` 的上框線分隔，不是各自的外框／底色／圓角。教練模式：項目卡（有
編輯／上下移／刪除工具列）維持分開畫在上面，紀錄卡畫在下面、不重畫標題（塞不進合併
的卡片）。

**未來的日子**：13b 原本只規定「只能預先排主動休息」，程式碼卻只擋了狀態按鈕，沒擋
數字欄跟「完成」——併卡的過程中補上（`showNums`／`showDone` 都加了 `!isFuture`），
不然未來的日子會冒出一個可以預先打勾的「完成」按鈕。

**反悔成本**：中。資料格式沒變（`status` 少了一個合法值，讀取端相容舊資料）；render
函式砍掉重排，回退要整段復原，但 CSS／JS 都在 git 歷史裡。

## 24. iOS 數字欄位縮放縮不回去：照搬 babylog 的 resetZoom()

使用者實測（2026-09-13）：填數字時畫面會被 iOS Safari 放大，選別的地方不會自動縮回去，
並註明「babylog 有修過了」。查證：babylog 的 `js/app.js` 早就有一模一樣的問題跟修法
（`resetZoom()`），註解直接寫著原因——iOS 在 focus 的欄位字型小於 16px 時會自動放大，
理論上失焦要自動縮回，但**當失焦的原因是「整個畫面被 JS 換掉」而不是使用者悠悠點開
別處時，縮放常常縮不回去**。MarathonTrain 幾乎所有輸入框（`.rec-num`、`.goal-input`、
教練編輯表單…）都比 16px 小，且整個 App 的 render() 就是「整份 innerHTML 換掉」，
完全符合這個誘發條件。

**做法**：照搬 babylog 的技巧——短暫把 viewport 的 `maximum-scale` 壓到 1.0 強制縮回，
350ms 後還原，使用者之後照樣能正常雙指縮放。差別是 babylog 只在一個特定時機呼叫（關閉
某個面板），MarathonTrain 的 `render()` 呼叫頻率高得多，直接照搬會在短時間內連續呼叫、
把 `, maximum-scale=1.0` 疊加好幾次，且每次都把「已經疊過的內容」誤存成要還原的
`original`——**縮放永久鎖死**，比原本的問題更糟。用同一個計時器＋只在第一次呼叫時記
原始內容，後續呼叫只延長還原時間，不重複疊加：`resetZoom()` 放在 `main.js` 的
`render()` 裡，每次 `#root` 被換掉都呼叫一次，沒放大時這行沒有任何視覺效果。

**反悔成本**：低。純顯示層修正，不碰任何資料。

## 25.（待她確認）本週頁二擇一的日子退化成逐項打勾——疑似 `planOverrides` 舊資料

使用者截圖顯示：第 1 週週日「完全休息 或 散步＋伸展」（課表本身是二擇一）沒有顯示成
第 23 條設計的並排選項卡，而是逐項打勾的清單（跟教練模式的項目卡、或非二擇一的多項目
日子一樣的畫法），且「完全休息」被單獨打勾後仍然顯示「實際分鐘」欄位。

查證：`data/plan.json` 這天 `selectOne: true` 沒有變過（`tools/verify_plan.py` 47 項
持續通過）；本機用乾淨帳號（沒有 `planOverrides`）測試，這天正確顯示成二擇一，
選了「完全休息」後公里／分鐘／體感強度都正確隱藏。`Store.effectiveWeek()` 的邏輯是
「`planOverrides/{週}` 存在就整週改讀那份文件，不存在才退回出廠 `plan.json`」——這是
教練模式的共用課表覆寫層，三個白名單帳號誰都能寫。**唯一能讓同一天在同一份出廠資料下
變成非二擇一的原因，是某次教練模式操作把 `planOverrides/1` 存成了 `selectOne: false`**
（`toggleDaySelectOne` 本身沒有 bug——單純的開關，我看過程式碼；比較像是這次很長的一輪
測試過程中，某次教練模式操作誤觸了這個切換，存到她的真實帳號上，不是本機測試資料）。

這是**她 Firestore 上的共用課表資料**，我沒有她的 Google 帳號，沒辦法直接進去改
（也不該用服務帳號金鑰繞過——見專案隱私原則）。修法：打開教練模式 → 本週頁切到
第 1 週 → 點開週日那一列 → 會看到「這天是「二擇一」」的切換開關，把它打開。
這個文件是三人共用的，任何一個白名單帳號都能修。

**反悔成本**：無——純資料修正，跟安全性、跑量計算都無關。

## 21. 體感強度拿掉高低階梯，改成等高＋四色

使用者看到體感強度那四組「輕鬆矮、全力高」的階梯圖（模仿 Apple Fitness 的視覺），
要求（2026-09-13）：「這裡不用呈現高中低的 graphic，改成等高但有四個強度顏色」。

裁決：四組維持分組跟十個點的顆粒度不變，只是拿掉 40%/60%/80%/100% 的高度差，
四塊等高，改用底色分輕重：輕鬆綠、中等黃、困難橘、全力紅。顏色**沿用 App 既有的
`--accent`（本來就代表「正常」）／`--warn`（本來就代表「留意」）／`--crit`（本來就代表
「警示」）**，只新增一個「困難」用的 `--hard`／`--hardBg`（warn 跟 crit 中間的橘色）——
不另外發明一套色票，跟現有的「已同步」綠點、安全提醒的警示色是同一套語言。

**選中／課表建議的視覺**：階梯拿掉之後不能再靠「變大變高」分辨選中的那格，改成：
- 未選：該等級顏色，35-45% 透明度的實心點
- 選中：實心＋深色外框（`box-shadow`，不佔版面空間，不會把旁邊的點擠開）
- 課表建議的 RPE 區間：改成**外框而非填色**——填色的話會跟這一格本來的等級顏色打架，
  外框可以疊在任何底色上都看得出「這裡被圈起來」

「打得比課表高」的提示文字跟邏輯不變（第 17 條）。

**反悔成本**：低——純 CSS／render 的顯示層改動，`entries.effort` 的資料格式、
`weekAdjustments`、跑量計算完全沒動。

---

## 26. 教練模式的常用項目庫：常用項目（複製式）＋自訂動作清單＋自訂影片

使用者問（2026-09-13）：教練排課時很多東西是重複的，能不能重新用。討論了三個方案——
A 常用項目庫（複製式）、B 複製到其他週、C 連動範本（改一處全部跟著變）——她選 **A**，
並確認：肌力跟跑步都會重複、**複製式**、**要能在 App 裡新增動作清單跟影片**、主要教練
模式用但**誰都能存取**。

**C 不做的理由**（記下來，免得之後又被提）：改一次範本就可能悄悄改掉好幾週的負荷（第 0 條）；
已經過去的日子也會跟著被改，歷史紀錄不準；還要處理「只改今天以後」這類例外。

**做法**：新的共用 Firestore 集合 `library/{id}`，規則跟 `planOverrides` 一樣 `isMember()`
可讀寫（第 11 條「三人皆可、不限定誰是教練」）。三種文件：
- `kind:'item'` 常用項目：教練模式的項目卡多一顆「存成常用」，把整套設定存一份；「＋新增
  項目」的表單最上面多一個「從常用項目帶入」。**帶入是深拷貝**，拿到新的項目 id，之後改
  範本不會改到已經排好的日子；設定頁可以改名、改內容、刪除。
- `kind:'workout'` 自訂動作清單、`kind:'video'` 自訂影片：跟 `data/workouts.json`／`videos.json`
  同形狀，出現在項目編輯表單的下拉裡（標「自訂」）。**這兩種是被引用的**（項目記 id），
  跟內建的一樣——改了動作清單，用到它的日子看到的就是新版，這是刻意的，跟常用項目的複製式不同。
- 影片可以存搜尋關鍵字，或貼上 YouTube 網址。`videos.json` 的規則是「不要生成 YouTube 網址」——
  這裡是教練自己貼的真實網址，不是程式產生的，不違反。網址**只收 `https://`**（Store 寫入跟
  畫面產生連結兩處都擋），`javascript:` 這種 href 會變成可以執行的連結，`h()` 擋不了。

**刪除是軟刪除**（`deleted:true`）：從清單跟下拉拿掉，但已經被課表引用的動作清單／影片照樣
查得到、照樣顯示——硬刪會讓排好的項目安靜地少掉「查看動作」。編輯表單遇到引用了已刪除
（或這台裝置還查不到）的自訂項目，會保留成一個選項，存檔不會把引用洗成「（無）」。

**對抗式審查抓到、已修的**：
1. `applyPatch` 會無條件塞 `deleted:false` 但不蓋時間戳——既有文件的編輯跟別台幾乎同時的刪除
   撞在一起時，雲端值跟時間戳對不上，各台合併結果不一致，重新整理後刪掉的又跑回來。
   既有文件改成完全不送 `deleted`，已刪除的文件拒絕再存。
2. 沒登入時存的東西永遠不會推上雲：第一次拿到**伺服器確認過**的快照時補推（雲端沒有的整份推，
   雲端有的只推本機時間戳比較新的欄位；不拿離線快取比，免得用本機舊欄位蓋掉別人剛改的）。
3. 從 Firebase Console 手動改壞的文件（少了 `exercises`／`item`）會讓整個設定頁連教練模式開關
   一起渲染失敗：讀取時做形狀檢查，壞的一律當沒有（跟 `_isValidWeekShape` 同一個精神）。
4. 見上面「編輯表單保留查不到的引用」。
5. 庫的訂閱出錯後不會自己重掛：「重試」（`Sync.resubscribe`）一起重掛；「還沒開通」的提示
   多一顆「發布好了，重新讀取」。

**規則沒發布時不能把人登出**：`library` 讀取被拒走 `_handleSnapErr` 通用分支的話會判成
「帳號未授權」直接登出——只是規則少貼一段，三個人全被踢出去。改成只影響這個功能、顯示提示。

**需要使用者做的**：把 `firestore.rules.local` 整份重新貼到 Firebase Console 發布（多了
`library` 那一段）。

**反悔成本**：低。新集合、新畫面，不動既有課表資料格式；不要的話拿掉設定頁的面板跟兩個
按鈕即可，已經被引用的自訂動作清單／影片要先換回內建的。

## 27. 總覽頁「我的進度」的階段條重畫成一週一格的時間軸

使用者說（2026-09-14，附總覽頁截圖）：「這 UI 重新設計一下，不同期的太亂了」。

**舊版亂在哪**：
1. 每個階段一塊色塊，塊裡的「進度」是一塊比底色**更深**的色塊，蓋在階段名稱上，看起來像破洞，
   不像進度。過去、現在、未來三種階段長得差不多。
2. 設了 `min-width:480px`，手機上要左右捲，最後的「賽週」被切掉。
3. 下面還有一條整體進度條，跟上面的色塊講同一件事。

**新版**：一張卡，三層。
- 上面一行：目前階段名稱（粗）＋「本階段第 N 週／共 M 週」。還沒開訓／計畫結束／賽週各有自己的說法。
- 中間：26 週一週一格，照階段分組（組內 2px、組間 6px 空隙）。過去的週填淡色、這週是深色而且
  比較高、未來是底色。階段名稱寫在格子下面，目前的階段加粗上色、過去的變淡。
- 下面一行：「下一階段：基礎期，第 9 週開始」。
- 拿掉整體進度條（上面的「目前週次 / 26」磚已經有了）。

**賽週只有 1 週、減量期只有 3 週，名稱會比格子寬**：組寬用 `flex-grow`＝週數，最小寬度交給
flex 預設的 `min-width:auto`（＝自己的階段名稱寬度），比名稱窄的組停在名稱寬、剩下的空間再照
週數分給其他組。不寫任何「剛好對得上某個寬度」的數字。代價是賽週那一格比一般的週寬一點點，
可以接受（它本來就是最特別的一週）。

**驗證**：375px 寬、深淺兩色、還沒開訓／第 12 週／第 24 週／第 26 週／結束後五種狀態，
都沒有左右捲、沒有名稱被切掉。

**反悔成本**：低。只動 `renderPhaseTimeline` 跟它的 CSS，不動資料。

## 28. 一個項目可以掛好幾部影片

使用者問（2026-09-14）：「如果我想加兩個以上的影片參考可以嗎？」提了兩個方向——A 一個項目掛多部、
B 動作清單裡每個動作各自掛——她選 **A**。

**資料**：項目多一個 `videoRefs` 陣列。舊的單一 `videoRef` **不拿掉**，新版存檔兩個都寫，
`videoRef`＝第一部：還開著舊版網頁的裝置（沒重新整理）至少看得到一部。**讀取一律走
`PlanData.itemVideoRefs(item)`**：`videoRef` 排第一、再接 `videoRefs`，去重。兩邊對不上時
（舊版網頁只改了 `videoRef`，或有人在 Firebase Console 手改）取聯集——寧可多一部，不要安靜少一部。
出廠 `plan.json` 不動（只有 `videoRef`），讀取端相容。上限 10 部（Store 跟表單兩處擋）。

**表單**：影片參照一部一列下拉＋✕，底下「＋ 再加一部影片」。加列／拿掉列**直接改 DOM、不重繪**——
表單其他欄位打到一半的字不在 state 裡，`render()` 會把它們洗掉；新的一列從表單裡的 `<template>`
複製（`<template>` 的內容不在 DOM 樹上，存檔時 `querySelectorAll` 不會多讀一部）。選「（無）」的列
當沒填，重複的只留一次。查不到的自訂影片（庫還沒同步到這台）照舊保留成那一列的選項。

**卡片**：每部影片一個連結，連結上直接寫影片名稱（原本貼網址的寫「看影片」——兩個並排分不出來）。

**舊版網頁的邊界**：舊版網頁編輯同一個項目並存檔，只會留下第一部（它的表單只有一個下拉，整個
項目用表單內容取代）。常用項目庫走 `set(merge:true)`，舊版改範本時 `item.videoRefs` 會留在雲端、
`videoRef` 被換掉——讀取時取聯集，不會少。重新整理就是新版，這只會發生在更新的空檔。

**反悔成本**：低。拿掉表單的多列、讀取改回只看 `videoRef` 即可；已經存了多部的項目會只剩第一部。

## 29. 「查看動作」一秒後自己收起來；整張卡點了就打勾

使用者回報（2026-09-14）兩件事：點開「查看動作」常常一秒後自動收合；只有點打勾的圓圈才應該打勾，
現在點框的任何地方都會觸發。

**收合的成因**：`render()` 每次都把 `#root` 整個換掉，`<details>` 的開關狀態只存在 DOM 上，任何
一次重繪都會收起來。「一秒後」是因為 v0.11.0 的常用項目庫訂閱用了 `includeMetadataChanges`，**每個
快照都無條件 `_notify()`**——開 App 時「離線快取 → 伺服器確認」只有 metadata 變，也整頁重畫一次。

**修法（兩層）**：
1. 開關狀態放進 `App.state.openDetails`（key＝`wo:項目id`），`<details ontoggle>` 記下來、render 時
   帶 `open` 屬性。這是根治：以後不管什麼原因重繪都不會收起來。項目 id 全課表唯一，對調日子時跟著內容走。
2. 庫的快照只在「刪了文件」或「從讀取被拒恢復」時才 `_notify()`；有文件內容變動時 `Store.mergeRemoteLibrary`
   自己會通知。其他四個個人資料的訂閱**沒改**——它們的 metadata 決定同步膠囊顯示「同步中／已同步」，
   那個重繪是需要的，第 1 層已經讓它不會收起「查看動作」。

**只有圓圈能打勾**：教練模式的項目卡（整張 `.item` 有 onclick）跟一般模式二擇一的選項卡（整張 `.rec-opt`
有 onclick）改成只有圓圈是按鈕。二擇一的圓圈從 15px 放大到 22px；三種圓圈都用 `::after` 往外撐出點擊範圍（圓圈外緣再往外約 7px——
`inset:-9px` 是從 padding box 算，扣掉 2px 邊框），不動版面。選中的二擇一圓圈改用 inset 陰影畫，
不加粗邊框：邊框一加粗點擊範圍就跟著縮，而選中的圓圈是唯一能取消選擇的地方（對抗式審查抓到的）。外層不再可以點，`itemPlanParts` 裡連結跟 `<details>` 的 `stopPropagation` 一起拿掉。

**反悔成本**：低。純畫面行為，不動資料。

## 30. 心率區間一律用 Zone 表示

使用者指示（2026-09-14）：「心律區間只用 zone 的形式，幫我改掉 app 裡有的相關內容，包括 .md 檔。」

**換算基準**：原文第二節是「佔最大心率」的百分比，照最常見的最大心率五區換算——
Zone 1 50-60%、Zone 2 60-70%、Zone 3 70-80%、Zone 4 80-90%、Zone 5 90-100%。
區間上限剛好在邊界上不算進下一區；跨區寫「Zone 2-3」。文件裡只有下面這張對照表還寫原本的百分比，
留著是為了說明換算怎麼來的（程式碼註解跟 `verify_plan.py` 的守衛也會提到換算基準）。

| 原本 | 現在 | 精確嗎 |
|---|---|---|
| 恢復 50-60% | Zone 1 | 完全一樣 |
| Zone 2 有氧跑 60-70% | Zone 2 | 完全一樣 |
| 長跑 65-72% | Zone 2-3 | **變寬**：Zone 2-3 是 60-80% |
| 節奏跑 70-75%（推導） | Zone 3 | **變寬**：Zone 3 是 70-80% |
| Marathon Pace 75-80% | Zone 3 | 下限變寬：Zone 3 從 70% 起 |

**第 0 條要攤開講**：長跑跟節奏跑的心率**上限從 72%／75% 變成 80%**。這是表示法本身的精度，
不是加量——課表的 RPE（長跑 5-6、節奏跑 5-6）跟強度說明（「後段稍吃力但仍能斷句對話」）沒變，
實際上限靠這兩個守。節奏跑跟 MP 同在 Zone 3，靠 RPE 5-6 vs 6-7 區分。她的心率錶自己算區間
（Garmin 預設就是這五區；Apple Watch 預設用心率儲備，邊界會不太一樣），以錶上顯示為準。

**改了什麼**：
- `tools/build_plan.py` 的強度表跟三段備註（「長跑強度進到…」「MP 配速（…）」「沿用長跑…」）改寫 Zone，
  重新產生 `plan.json`——diff 只有 152 個心率欄位跟 9 則備註，其他一個字都沒動。
- `tools/verify_plan.py`：既有斷言改成 Zone；**新增兩條守衛**：心率欄一律符合 `Zone N` 或 `Zone N-M`、
  資料檔裡不能出現原文那五個百分比寫法。節奏跑的「推導強度有標記」原本用心率字串認，Zone 3 跟 MP
  撞在一起，改用類型認。
- 畫面：顯示一律走 `PlanData.fmtHeartRateZone()`。**Firestore 裡的舊資料不遷移**：教練改過的週、常用
  項目庫還存著「60-70%」，讀的時候換算成 Zone 顯示；教練下次存那個項目就存成 Zone。
- 教練表單的心率區間從自由輸入改成下拉（Zone 1、Zone 1-2 … Zone 5）。舊的百分比預選換算後的 Zone；
  換算不了的舊文字（手打的「輕鬆」之類）保留成一個「（舊寫法，請改選）」選項，不會存檔時被洗掉。
- 節奏跑的來源標籤從「（內插）」改「（推導）」——換成 Zone 之後不是兩個數字中間取值，是往上一區。
- 階段目標卡的 Zone 2 參考從「這階段 Zone 2 對應心率 60-70%、RPE 4-5」改成「這階段的 Zone 2 體感：
  RPE 4-5、輕鬆對話」——心率只寫 Zone 之後「Zone 2 對應心率 Zone 2」沒有資訊。
- 文件：本檔第 6、12、19、22 條、`日曆基準.md`、`CHANGELOG.md` 的歷史條目，百分比一律換成 Zone
  （恢復跟 Zone 2 換算前後一樣；長跑、節奏跑、MP 換成 Zone 之後範圍變寬，原本的百分比只留在上面的對照表跟 git 歷史）。本機的 `docs/計畫原文/訓練計畫.md` 第二節表格跟公式那一行、
  `開發規格書.md` 的範例也改了；改之前整份備份在 `docs/計畫原文/_備份-2026-09-14-改Zone前/`
  （都在 gitignore 裡）。`verify_plan.py` 逐字比對的四段安全文字不在改動範圍內，照樣通過。
- **沒改**：`docs/_mockups/week-page-redesign.html`（舊設計的示意稿，不是 App）；`planVersion`
  不升——出廠課表的內容沒變，升了只會讓教練改過的週無故跳出「基於舊版」的提醒。

**反悔成本**：低。`fmtHeartRateZone` 讀取端換算、備份都在；改回百分比要重寫產生器的強度表與斷言。

## 31. 未來的日子被誤觸打勾，而且改不回來

使用者回報（2026-09-14，週一凌晨，附截圖）：週六 9/19 的「長跑（Zone 2）」顯示「照表 · 已完成」，
「打勾誤觸了，沒辦法改回來，而且還無法輸入跑了多久多遠」。

**成因**：第 13b 條說未來的日子只能預先排休息、不能預先打勾。v0.10.0 只把紀錄卡的「完成」按鈕
在未來的日子藏起來，**真正會寫入的地方沒擋**：多項目日子的勾、二擇一的圓圈、教練模式項目卡的勾
（當時整張卡都能點，就是第 29 條那個誤觸）都能把未來的日子打成完成。打了之後，一般模式唯一能取消
的「完成」按鈕剛好是藏起來的——卡死。數字欄在未來的日子本來就不顯示（13b），這部分是設計，不是 bug（第 38 條起改成未來的日子也顯示）。

**修法**：
1. 守在 `Store`（唯一的寫入口）：`canMarkDoneAhead(dateKey, itemId)`——今天以前一律可以；未來的日子
   只有休息類的項目可以（＝預先排休息）。`toggleItemDone`、`setSelectedItem` 在「打上去」時檢查；
   **取消永遠允許**。`setActualStats` 在未來的日子不自動打勾。
2. 畫面：未來的日子還沒勾的圓圈一律停用、變淡（多項目的勾、二擇一非休息的選項、教練模式項目卡）；
   已經勾了的照樣能點掉。單一項目的日子如果已經被打成完成，「完成」按鈕照樣出現（亮著），上面一行
   說明「這天還沒到，應該是誤觸了——再點一下就能取消」。

**畫面跨日要重畫**（對抗式審查抓到的）：「未來」是 render 當下判斷的。iOS 會把主畫面 App 整晚留在
記憶體，隔天早上畫面還是昨天畫的——今天的圓圈照樣停用、點了沒反應。`main.js` 記下上次畫面是哪一天，
切回 App（`visibilitychange`）、從快取恢復（`pageshow`）、跨過午夜（計時器）時日期換了就重畫。

**她那一筆**：更新後打開週六，點「照表完成」就取消了。

**反悔成本**：低。寫入端多一個條件、畫面多一種停用狀態。

## 32. 影片一律加 ▶ 圖示；換上正式 App icon

使用者要求（2026-09-14，附截圖）：「只要是影片，都幫我在名稱前面加一個小的 svg 播放按鈕 icon，這樣使用者才能分辨」，
截圖裡的「內核心呼吸」連結前面沒有圖示。另外給了一張 TOKYO 2027 跑者設計圖要換成 App icon。

**圖示看不到的成因**：程式碼其實一直有放 `ICON.play`，但 CSS 沒有給 `.item-link svg` 尺寸。svg 只有 `viewBox`
沒有寬高，在 `inline-flex` 的連結裡被旁邊的標題擠成 0 寬——從有影片連結以來就是這樣，不是 v0.12.0 才壞的。
補上 `flex:none; width/height:12px`。

**改了什麼**：
- 卡片上的影片連結一律 ▶ 開頭。搜尋型原本是放大鏡，改成 ▶，文字照舊寫「搜尋「…」」——使用者要的是一眼分得出
  「這是影片」，是不是搜尋由文字講。
- 設定頁常用項目庫的影片清單，名稱前也加 ▶。
- 教練表單的影片下拉選單沒有加：`<option>` 裡放不了 svg。

**App icon**：新增 `tools/make_icons.py`。設計圖是「白底上一個圓角方塊」，直接縮小會變成 icon 裡又一個小方塊加白邊，
所以腳本先裁出方塊、四個圓角外面用鄰近顏色補滿（iOS／Android 自己會套圓角遮罩），存 1024 母版
`icons/icon-1024.png`，再縮出 index.html 跟 manifest 用到的六個尺寸。圓角外面的判斷用「量左上角的圓角形狀、四角對稱套用」，
不用「從角落沿白色往內擴散」——第一版用擴散，圖案右下的白色跑道線碰到邊緣被當成底色吃掉、糊掉。
favicon 原本的 🏃 emoji SVG 拿掉，改用同一張圖的 32px。manifest 的連結跟 icon 也加上 `?v=`。

**已經加到主畫面的 iPhone 不會自動換 icon**：iOS 只在「加入主畫面」那一刻抓圖，要移除再重新加入。

**反悔成本**：低。

## 33. 教練可以在 App 裡改內建的動作清單跟影片；改動只從今天起生效

使用者要求（2026-09-14）：「應該要有個地方可以讓教練可以手動改這些內容才對」——內建的 4 份動作清單
（data/workouts.json）跟 8 支影片（data/videos.json）原本 App 裡改不了，只能自己另建一份再逐天換引用。

規劃先跑了三個鏡頭（資料同步／手機操作／第 0 條與歷史）的對抗式審查，結果逐條回原檔核對過。再請她裁定四件事，
她的回覆（原文）：「1. 只套用到後面的週數，前面不動」「2. 不作為」「3. 不改前面的」「4. 可以」。解讀：
- **範圍**：改動從「今天（含）」起生效，今天以前的日子維持改之前的內容。沒有「全部一起改」。
- **加量提醒**：不做。改完變重不跳提醒也不擋——跟第 22 條階段目標同一個立場，教練的判斷。這是她知情的裁定，
  記在這裡（跟第 12 條同理：第 0 條管的是休息／受傷的調整，不是教練對課表本身的決定）。
- **過去的日子**：顯示當天的舊版本，不會被改。
- **內建影片**：一起開放。

**資料：版本。** 動作清單跟影片都有 `history: [{until, content}]` 跟 `from`。某一天用哪個版本：history 裡
第一個「那天 <= until」的；都不符合就是現在的版本。同一天再改直接取代今天的版本，不多開一個。
- 內建的修改版：`library/{內建id}`，`kind:'workoutOverride'`／`'videoOverride'`，`{content, from, history}`；
  `content:null`＝這段期間用內建的（「還原內建」也只從今天起）。**用新的 kind**：舊版網頁的讀取檢查不認得就整份
  略過，不會被當成一份自訂清單列出來、還附一顆會把大家的修改版刪掉的「刪除」（審查抓到）。
- 自訂的（`kind:'workout'`／`'video'`）：內容照舊放最上層（舊版網頁讀得到最新版），history 另外存。
- **不用軟刪除表示還原**：第 26 條的 `saveLibraryDoc` 刻意拒絕再存已刪除的文件，用它表示還原會讓那份內建
  清單永遠改不了（審查抓到）。

**寫入：** 內建的修改版一定要登入才能存，用 transaction 跟打開編輯器那一刻的版本比對（跟第 11 條
`pushPlanOverride` 同一套），兩個人同時改，後存的人會被擋下並提示。整份文件寫入、雲端來的整份取代本機，
不逐欄位合併（content 跟 history 必須一起對）。寫入失敗（沒網路、權限、衝突）本機還原成改之前，不會出現
「這台看起來改好了、其他人沒收到」。自訂的沿用第 26 條的逐欄位合併，只多寫 history。

**固定的安全提醒。** 審查發現：「出現漏尿或下墜感就退回 basic」這句只藏在 advanced 的「推導值」說明裡，
教練一存檔就會從 28 天的畫面上消失；而「負荷說明」欄位從來沒有顯示在任何畫面上。改成：
- `data/workouts.json` 每份加 `safetyNote`，畫面一律從 JSON 拿，修改版蓋不掉；編輯器上方顯示「固定的安全提醒（不能改）」。
  `verify_plan.py` 守「每份都有、講到漏尿跟下墜感」「進階版的提醒指得到基礎版的名稱」。
- 「查看動作」最上面顯示負荷說明。
- 「推導值」說明只在顯示 JSON 版本的日子出現（教練改過＝確認過，第 19 條）。

**畫面：**
- 設定頁常用項目庫：動作清單跟影片都列出內建的，標「內建」或「內建·已修改」，每份寫「用在 N 天（今天起 M 天）」，
  按鈕「編輯」，改過才有「還原內建」。
- 本週頁教練模式：「查看動作」裡多一顆「編輯這份動作清單」，編輯器直接取代那張卡。換天、換週、換檢視、打開
  項目表單都會關掉它（兩個表單同時開，一邊的重繪會洗掉另一邊還沒存的字）。
- 編輯器上方講清楚「今天（9/14）起用到它的 N 天會換成新內容；今天以前的 M 天維持原樣」；沒登入時儲存鈕停用並說明。
- 動作可以上下移；動作備註跟負荷說明改成多行、限制字數（原本超過 200 字存檔時會被悄悄截掉）。
- 影片的連結方式多一個「不需要連結（只是說明）」（內建的「死蟲式＋鳥狗式」就是這種，原本編輯器表示不了）。
- `verify_plan.py` 另外守「內建 id 不以 c- 開頭、影片跟動作清單不撞 id」——修改版用內建 id 當文件 id。

**實作後的對抗式審查抓到、已修的**（每條都有測試）：
1. 存不進雲端時「退回存檔那一刻本機的樣子」是錯的：那份可能是上一次也沒存進去的樂觀寫入（離線時連按兩次），
   或比存檔期間收到的別人新版還舊。改成退回「雲端確認過的最後一份」（快照或重新讀取來的），失敗後再讀一次雲端校正；
   同一份還沒存完不能再存；打開編輯器記的基準版本也改用雲端確認過的，不用本機樂觀寫入後的。
2. 按下儲存編輯器就關了，雲端存不進去時改的內容全部不見 → 失敗時把編輯器連同草稿打開回來，跳出原因。
3. 衝突時同步膠囊顯示「離線，點擊重試」，真正的原因看不到 → 衝突直接跳訊息，不動膠囊；沒網路才顯示離線，之後存成功會恢復。
4. 已刪除的自訂清單，卡片上還有「編輯這份動作清單」，存檔只會說「內容不完整」→ 不顯示按鈕，存檔講清楚原因。
5. 內建清單沒改任何東西就按儲存，會被標「內建·已修改」→ 內容跟內建一樣就不存。
6. 讀取被拒（規則沒發布）時編輯器寫「要先登入」→ 分開講。
7. 影片選「不需要連結」在卡片上什麼都不顯示 → 顯示成純文字（沒有播放鍵）。

**已知、不修的缺口**：還開著 v0.12 舊版網頁（沒重新整理）的裝置如果改了**自訂**動作清單或影片，舊版不會寫 history，
改動會連今天以前的日子一起蓋掉；下一次新版再改時，那段期間會記成舊版改的內容。三個人、只發生在更新的空檔，重新整理就沒事。
內建的修改版不受影響（舊版完全讀不到新的 kind）。

**反悔成本**：中。資料格式多了 history／新 kind；拿掉功能的話修改版文件留在雲端，舊邏輯會忽略它們、回到內建。

## 34. 影片圖示改成實心圓播放鍵

使用者回報（2026-09-14，附截圖）：影片前面的 ▶ 跟「查看動作」前面展開用的 ▶ 太像，要改成「一個圓圈，反白呈現播放鍵」。
改成實心圓（文字色）＋挖空的三角形（填所在卡片的底色），16px。卡片的影片連結跟設定頁的影片清單都用這個。

## 35. 沒登入時，本週頁講清楚「看到的是出廠課表」

使用者回報（2026-09-14，附兩張截圖）：Safari 網頁直接打開看不到影片，加到主畫面的看得到。

**成因不是程式錯誤**：Safari 那張右上角是「點擊登入以同步」，主畫面那張是「已同步」。iOS 的 Safari 分頁跟主畫面
App 是兩份分開的儲存空間，登入狀態不共用。那部影片是教練在第 2 週改課表時加的——教練改過的週（planOverrides）
跟自訂影片（library）都在雲端，沒登入讀不到，看到的是出廠課表。兩張截圖的本週跑量目標也不一樣
（6.7–8.9 km 是出廠值，7.2–11.7 km 是改過的），可以對得上。

**改的**：原本只有右上角一顆小小的「點擊登入以同步」，看不出「現在看到的內容不完整」。本週頁最上面加一條提示：
「還沒登入，看到的是出廠課表——教練改過的內容、自訂的影片跟動作清單要登入後才看得到」，附登入按鈕。登入中不顯示；開 App 時要等 Firebase 回報過一次登入狀態才顯示（不然已登入的人每次開 App 都會閃一下，還會把今天那列擠到頂欄底下）。登出之後記憶體裡還留著共用內容時，改說「看到的課表可能不是最新的」。

## 36. 點進打字框一律不縮放

使用者要求（2026-09-14）：「打字框點擊進入打字狀態，一律不縮放」。第 24 條照 babylog 做的是「放大之後縮回來」，
她要的是根本不要放大。

**做法**：`index.html` 的 viewport 固定加 `maximum-scale=1`。iOS Safari 在字小於 16px 的欄位被點到時會自動放大，
這個設定會擋掉。Safari 分頁裡從 iOS 10 起不擋使用者自己雙指縮放（Apple 為了無障礙刻意忽略）；
主畫面 App 模式能不能雙指縮放**還沒實測**，如果不行、又需要放大，再改成「輸入框字改 16px」的做法。
另一個常見做法是把所有輸入框的字改成 16px 以上——這個 App 的數字欄、目標欄都是窄欄小字，全部放大會擠壞版面，不採用。
第 24 條的 `resetZoom()` 留著當備援。

**沒辦法在這邊驗證**：自動放大是 iPhone Safari 的行為，桌機的模擬器重現不了，要在手機上實際點點看（Safari 分頁跟主畫面 App 各一次）。

## 37. 本週頁展開的那一天整塊一個底色；數字欄跟「更換項目」拉開

使用者要求（2026-09-14，附截圖）：「本週頁展開時，底色可以延伸到內容，這樣看起來比較像當天展開的內容」、
「實際分鐘的填寫框，太接近項目更換按鈕，要調整一下間隔」。

- 底色原本只在展開那一列的標題（`.weekday-row`），底下的紀錄卡看起來是另一塊。改成整個 `.weekday-acc.open`
  一塊 `--card2` 底、圓角，標題列跟內容包在一起；紀錄卡維持 `--card`，在底色上浮出來。教練模式的項目卡本來也是
  `--card2`，放在同色底上會糊掉，改成 `--card`（打勾、編輯中的顏色不變）。
- 數字欄跟「沒照表？更換項目｜自主休息」在同一個區塊裡、中間沒有間距。兩者之間加 14px；更換項目時順序反過來
  （按鈕在上、數字在下），也一樣有間距。

**反悔成本**：低。純 CSS。

## 38. 今天以後的日子也有實際公里／分鐘的填寫框

使用者回報（2026-09-14，附週五 Zone 2 跑、週六長跑的截圖）：跑步日沒有時間跟距離的輸入框，以為哪一版改壞了。

**不是改壞**：v0.10.0（第 23 條）起未來的日子不畫數字欄，照 13b「未來的日子只能預先排休息，不能預先打勾」。
當時的考量是「填了數字就自動完成」，未來的日子有數字欄就等於能預先完成。但畫面上沒有任何說明，看起來像欄位不見了。
她的裁定：「從今天就開始，是今天之前的才不用動」——今天以後的日子也要有填寫框，今天以前的照舊。

**改的**：未來的日子也畫數字欄（`renderDayRecordCard` 的 `showNums` 拿掉 `!isFuture`）。
**沒改的**（13b、第 31 條還是成立）：
- 填了數字**不會自動打勾**：`Store.setActualStats` 對未來的日子本來就不打勾（第 31 條加的守衛）。當天要按「完成」
  （或當天再改一次數字）才算完成。
- 「完成」按鈕照舊當天才出現；未來的日子被誤觸打勾時照舊會出現、可以取消。
- 「更換項目」在未來的日子照舊不能按，只能預先排「自主休息」。
- 體感強度照舊當天才出現（還沒練不會有體感）。

**要知道的**：未來的日子填的公里／分鐘會算進那週的「實際跑量」——週跑量把那週有填的數字都加起來，不看日期。

**反悔成本**：低。一行判斷式。

## 39. 內建的動作清單跟影片也能刪除，沒有例外

使用者要求（2026-09-14）：「內建的課表內容，也要是可以被刪除的，沒有例外」。第 33 條開放編輯內建內容時，設定頁的內建列只有
「編輯」「還原內建」，自訂的才有「刪除」。解讀成：4 份內建動作清單、8 支內建影片都要有「刪除」，包括第 33 條審查時特別提過的
產後骨盆底課程影片跟兩份骨盆底清單——她說「沒有例外」。

**刪除的意思跟自訂的一樣（第 26 條）**：從常用項目庫跟下拉選單拿掉，**已經排進課表的日子照樣顯示**。不然刪掉「重量訓練 A」，
25 天的重訓會安靜地少掉「查看動作」。確認視窗講清楚用在幾天、要從那些天拿掉請到那天把參照改成（無）。
自訂的刪了就沒了；內建的刪了會列在「已刪除的內建」，可以「恢復」——內建是整份課表的基礎，誤刪要救得回來。

**資料**：記在第 33 條那份修改版文件的 `removed` 欄位，不動 content／history（刪除不是改內容；恢復之後改過的內容還在，
刪除狀態下改內容或還原內建也不會把它恢復）。還沒有修改版的內建，建一份「content:null、沒有 history」的只帶 removed。
寫入跟修改版同一條路（要登入、transaction 版本比對、失敗退回雲端確認過的版本）。舊版網頁本來就讀不到這種文件。

**沒動的**：固定的安全提醒（safetyNote）照舊跟著清單走，清單還被課表用到，提醒就照樣顯示。
教練模式刪除「課表上的項目」的三個限制（一天至少留一個項目、二擇一至少兩個選項、不能刪二擇一裡唯一的休息）不在這次範圍——
那些是課表項目，不是常用項目庫的內建內容；要一起拿掉的話另外講。

**反悔成本**：低。

## 40. 新增「間歇跑」類型

使用者要求（2026-09-14）：「幫我加一項間歇跑項目」。解讀成教練模式項目表單的「類型」多一個「間歇跑」（`interval`）。

- 算跑步：週跑量目標加總（`RUN_TYPES`）、紀錄卡的實際公里欄都照跑步處理。`js/store.js` 跟 `tools/verify_plan.py` 的
  `RUN_TYPES`／`VALID_TYPES` 一起加（兩個執行環境各一份，靠註解同步）。
- **只加類型，不幫她排進課表、不預設強度**：出廠課表（原文）沒有間歇跑，這份是產後恢復的中度課表。間歇跑要排在哪天、
  心率 Zone 跟 RPE 多少，由教練新增項目時自己填——App 不替她決定一個會加量的預設值（第 0 條）。

**反悔成本**：低。拿掉類型之前，已經用了這個類型的項目會在表單顯示成要改選（跟舊的走跑類型同一套）。

## 41. 登入後自動選成那個帳號的人

使用者要求（2026-09-14）：「登入後自動選擇該 profile」，並給了三個帳號各是誰（mick／Annlin／Phoebe）。

**不能照 babylog 做**：babylog 把「哪個 email 屬於哪一家」直接寫在公開的 `js/firebase-sync.js`。這個 repo 是 public，
另外兩個人的 email 不能進程式碼（這個專案一開始就這樣定，對照表只放在 gitignore 的 `firestore.rules.local`）。

**做法：直接問 rules。** `firestore.rules.local` 的 `isSelf()` 本來就是完整的 email → userId 對照，而且
`users/{userId}/private`（身體狀況）只有本人讀得到。登入後對 `data/users.json` 裡每個人各讀一次 private
（limit 1、一定走伺服器——離線快取不看 rules），讀得到的那一個就是這個帳號，自動切過去（`Store.setActiveUser`，會重新訂閱）。
- 對照表只有一份（rules），加人時不會有第二個地方忘了改；前端不存任何 email。
- **只有「剛好一個讀得到、其他都明確被拒」才切**。離線、rules 對到零個（白名單內但 isSelf 沒這個人）或好幾個（設定錯），
  一律維持裝置上原本選的，跟以前一樣。
- 等結果的期間登出或換帳號，這次的結果作廢（序號比對），舊帳號的結果不能蓋掉新帳號的。
- 本機紀錄補推（`_backfillLocal`）改成等身分確認完才做：選錯人時先補推只會送出注定被 rules 拒絕的寫入，還會把那個人記成
  「這次補推過了」；登入前記的紀錄也要先搬到正確的人名下（見下面），補推才推得上去。切換身分的話由 `resubscribe` 補推新身分的。
- 設定頁「我是誰」在偵測到的那個人旁邊標「登入的帳號」；手動切換照樣可以（切到別人會照舊顯示身分不符）。

**對抗式審查抓到、已修的**（每條都有測試）：
1. 只有自動切換會清掉「身分不符」；手動切回正確的人或按重試，膠囊一整個 session 卡在「身分不符」→ 清除移到 `resubscribe`：
   任何重新訂閱都先清，身分還是錯的話 private 訂閱會馬上再設回來。換身分時（`resubscribe(true)`）也清掉上一個身分被拒的寫入
   跟「寫入被拒」，不然膠囊掛著上一個人的錯誤、這個人的同一天被標成「尚未同步」。
2. 新手機沒選過身分，預設是第一個人（mick）；登入前記的紀錄記在 mick 名下，自動切成 Annlin 之後畫面上看不到、也永遠不會上傳
   → **這台從沒選過身分、而且預設的那個人從沒在這台同步過**時，登入前的本機紀錄（打勾／數字、身體狀況、本週降量）搬給登入的人，
   跳一句說明。「同步過」記在 localStorage（自己的 entries 第一次收到伺服器資料時），同步過的是別人的雲端資料，絕對不搬。
3. 共用裝置：自動切到某人之後登出，那個人的身體狀況快取還留在裝置上 → 登出時，這次是自動選過去的身分就清掉他的身體狀況快取。
4. 分享連結跟總覽頁的說明還寫「自己選名字」→ 改成「登入會自動選；沒選到再手動選」。

**已確認**：`firestore.rules.local` 裡三個人的對照跟使用者給的一致（只比對 email 前綴，沒有印出來）。
**沒辦法在這邊驗證**：要真的用 Google 帳號登入，三個人各自登入一次看有沒有自動選對。

**反悔成本**：低。拿掉 `_detectIdentity` 的呼叫、補推改回登入當下做即可。

## 42. 跑步項目可以寫「訓練段落」：暖身、重複組、恢復、緩和

使用者回報（2026-09-14，附教練表單截圖）：「間歇跑通常是跑 400m x 4 次，前後加休息，目前的框沒辦法讓我規劃，請幫我重新設計」，
並要「Review 一下其他的跑步訓練，是不是有類似的狀況」。

**問題**：一個項目只有「總時長／總距離／心率／RPE」一組數字，間歇跑這種「暖身 → 4 ×（400 公尺＋恢復 90 秒）→ 緩和」的結構
沒有地方放，只能塞備註。

**其他跑步訓練的 review**（出廠課表裡同樣被塞進備註的）：
| 項目 | 週 | 原本寫在備註 | 原文有沒有具體數字 |
|---|---|---|---|
| 跑姿訓練日 | W9-12 | 高抬腿、後踢腿各 2 組 x 20 公尺 | 有（第七節） |
| 跑姿訓練日 | W13-16 | A-Skip、B-Skip 各 2 組 x 20 公尺 | 有（第七節） |
| 跑姿訓練 | W17-22 | 上坡衝刺（短距離、低強度版）、彈跳訓練（輕量） | 沒有 |
| 長跑主軸 | W17-22 | 可分段插入幾公里 MP 配速 | 沒有（「幾公里」） |
| 節奏跑 | W17-22 | 稍快於 Zone 2，10-15 分 | 只有總時長，沒有暖身緩和 |
| 喚醒跑 | W26 | 3-5K 含 2-3 段稍快的段落 | 沒有每段多長 |

原文有具體數字的（W9-16 跑姿訓練）直接寫成段落，備註**照舊保留**組數距離（見下面審查第 1 條）；原文沒有數字的**不補**
（轉檔不替她決定 MP 要跑幾公里），教練用新的段落編輯器自己排。

**資料**：項目多一個選填的 `segments`：
- 段：`{ kind, amount: {unit, min, max} | null, zone, note }`——kind 是暖身／主段／恢復／技術動作／緩和，
  單位是分／秒／公尺／公里，心率只能選 Zone（第 30 條），說明最多 60 字。
- 重複組：`{ kind: 'repeat', times: 1-50, steps: [段…] }`，組裡不能再放重複組（手機上巢狀兩層編不動，也用不到）。
- 上限：最上層 20 段、一組 10 段。
- **讀寫一律過 `PlanData.cleanSegments`**（教練表單、常用項目、Firebase Console 手改進來的都一樣），形狀不對的段直接丟掉，
  不會讓畫面壞掉。`verify_plan.py` 對出廠資料守同一套形狀。

**段落不算週跑量**：週跑量照舊用項目的總時長／總距離。段落常常時間跟距離混著寫（暖身 10 分＋4 × 400 公尺＋恢復 90 秒），
換算成公里要猜配速，而週跑量目標是第 0 條要守的數字，不能拿猜的東西算。表單上寫清楚「上面的時長／距離是整堂課的量」。

**編輯器**：表單的「強度說明」底下多「訓練段落（選填）」。一段三列（類型＋↑↓✕／數量～上限＋單位／心率＋說明），
重複組是虛線框「重複 N 次」＋組裡的段＋「在這組加一段」。底下「＋加一段」「＋加一組重複」「套用間歇範本」
（她舉的例子：暖身 10 分 → 4 ×（400 公尺＋恢復 90 秒）→ 緩和 10 分，心率不預設——第 40 條）。
加段、刪段、上下移都直接改表單、不重繪（跟影片列同一個理由）。常用項目會連段落一起存、帶入時一起複製。

**卡片**：meta 底下一段一行，類型是小標籤，重複組左邊寫「4 ×」、組裡的段用左框線包起來。

**對抗式審查抓到、已修的**：
1. 還開著舊版網頁（不認得 `segments`）的裝置存檔會把段落整個洗掉，W9-16 跑姿訓練的組數距離如果只放在段落裡就一起不見
   → 備註保留原本的組數距離文字（跟第 28 條 videoRef 兩個都寫同一個道理）。教練自己排的段落在更新空檔被舊版存檔洗掉，
   是跟第 28 條一樣接受的缺口，重新整理就沒事。
2. 教練在表單裡排段落時，同步快照或別人改課表觸發的重畫會把還沒存的段落、下拉選單洗掉（以前只擋「正在打字的輸入框」）
   → 項目表單或常用項目編輯器開著時，資料更新不重畫整頁；存檔、取消這些她自己的動作照常重畫，表單關掉時帶上期間的新資料。
3. 只選了心率的段（「恢復到 Zone 1-2」）存檔時被靜靜丟掉，整組都這樣的重複組也跟著消失 → 心率也算內容；
   重複組裡沒有任何內容時擋下存檔並講清楚。
4. 數量只擋「大於 0」，上限 100000 還在排序前檢查，「800 分」這種忘了換單位的手滑照存
   → 每種單位各自的上限（分 300、秒 3600、公尺 50000、公里 100），超過就擋下並問是不是單位選錯；上限在排序後檢查。
   編輯器底下即時顯示「段落合計：約 X 公里＋Y 分」，手滑一眼看得出來。
5. ✕（刪整組）跟 ↓ 貼在一起、23×30 px、沒有確認 → 工具鈕加到 36×36、✕ 拉開 8px，刪有內容的整組要確認。
6. `verify_plan.py` 說跟 `cleanSegments` 同一套規則，實際少檢查說明長度、心率選項、單位上限、段數上限 → 補齊。

**反悔成本**：低。拿掉編輯器跟顯示，`segments` 留在資料裡沒有人讀。

## 43. 教練模式也能拖曳（只限還沒開始的週）；項目庫可以新增常用項目、複製

使用者回報（2026-09-14，附教練模式桌機截圖）：「網頁版每天的項目無法拖曳」「跑步項目要在項目庫裡面也可以新增」
「項目庫裡面要能複製 Duplicate 項目，方便快速新增調整」。同一則還問了「間歇範本在哪裡」（回答，不改程式）、
提了「課程結構太複雜」（另外提方案給她選，這條不動）。

**拖曳不能用的原因**：不是網頁版壞了，是**教練模式**本來就不畫把手（第 18 條：教練模式下 `effectiveDayOrder`
一律回出廠順序，個人對調拖了也不會生效）。截圖是教練模式開著。

**做法**：教練模式的拖曳改成搬**共用課表本身**——把第 from 天整天的內容插到第 to 天，中間順移（跟第 18 條同一個插入語意），
存成那週的共用課表修改（`saveWeekOverride`，照舊有版本比對）。項目 id 不變。一般模式照舊是自己的本週順序。

**只限還沒開始的週**（週一在今天之後）。已經開始的週（含今天這週）不畫把手，提示「共用課表不能拖曳換天（已經打的勾會對不上）；
要換自己這週的順序，關掉教練模式再拖」。理由是資料模型：
- 打勾紀錄是「日期＋項目 id」。把今天的內容搬到明天，今天打的勾對不到任何項目，明天又出現一個「沒做」的同一堂課——第 0 條。
- 個人對調是「日曆格子 → 共用第幾天」。有人把週三的課換到週一做完了，教練再搬共用的週三，他週一那格就換成別的內容、
  做過的課跑到後面的日子又出現一次。別人的對調資料不一定載入到這台裝置，沒辦法事先檢查。

一開始做的是「今天以前的日子鎖住」，逐條推過上面兩個情境才改成整週判斷；程式也比較少（不用逐列鎖、不用擋拖放位置）。
還沒開始的週也可能有人先排好了自己的對調——那時還沒打過勾，只是看到的順序跟著變，不會加量。

**項目庫新增常用項目**：設定頁「常用項目庫」底下「＋ 新增常用項目」，打開跟本週頁同一張項目表單（含訓練段落跟「套用間歇範本」），
存成範本。以前只能從本週頁某個項目「存成常用」。

**複製**：常用項目、動作清單、影片每一列（內建的也有）都有「複製」，打開一份照原本預填、名稱加「（複本）」的**新內容**，
按存檔才建立、按取消什麼都不留，畫面自動捲到編輯器。內建的複製的是今天的版本；原本那份完全不動。

**複製不能繞過安全提醒**：內建動作清單的安全提醒一律從 JSON 拿、編輯蓋不掉（第 33 條）。複製出來的是自訂文件，
原本會沒有安全提醒——等於複製一份就能把提醒拿掉。所以複本記住 `copiedFrom`（只在新增時寫一次，之後的存檔不送），
顯示時照樣從那份內建的 JSON 拿安全提醒；複本的複本也跟著帶。

**對抗式審查抓到、已修的**：
1. 「複製」原本一按就存進三人共用的庫再打開編輯器，但內建那一列的複本編輯器排在整組最下面、在螢幕外，
   看起來沒反應會再按，每按一次多一份，取消也不會刪 → 改成上面寫的「預填的新內容，存檔才建立」＋捲過去。
2. 教練 A 開著表單（這時同步不重畫，畫面停在舊排列），教練 B 把同一週的天數搬了；A 存檔時是照「第幾天」存，
   會存到換過來的那一天，版本比對也抓不到（比對的是存檔當下的版本，不是畫面上那一版）
   → 搬天時共用課表寫 `layoutAt`；每次畫面畫完記下各週的 `layoutAt`，存檔時這週被別人搬過天就不存、請她再改一次。
   只有「搬天」會擋，別人改別的欄位照舊合併；自己剛存的算看過。

**審查提出、判斷不改的**：還沒開始的週也可能有「預先排的休息」（第 31 條允許未來的日子先勾休息、先按自主休息）。
搬天之後這些紀錄留在原本的日期——那是「那天我要休息」的日期意圖，不是「那堂課我不做」；搬過來的課在新的日子照常顯示，
不會疊課、不會加量。教練用表單改某天的項目本來就會有同樣的效果。

**反悔成本**：低。教練模式拖曳拿掉＝回到不畫把手；`copiedFrom`、`layoutAt` 沒人讀就是死欄位。

## 44. 課表項目改成「先挑常用項目、只調數字」

使用者回報（2026-09-14）：「課程結構應該修正，跑步表單裡面這些應該都要是在項目庫裡面才對，現在選了項目，還要選類型，
還又更多細項要填，整個課程調整變得太複雜了」。提了兩個方向給她選：
- A：課表項目連結到項目庫，改庫裡的內容今天起一起變。缺點是課表每週的數字會漸進（Zone 2 從 30 分到 50 分），連結式要嘛每週一個
  常用項目、要嘛當天還是得覆寫數字；出廠 228 個項目要改寫；也推翻第 26 條她選的「帶入是複製」。
- B：項目庫是定義項目的地方，課表只調數字。維持複製。

她選 **B**。

**新增項目**：「＋ 新增項目」第一步是「從常用項目挑一個」——庫裡每個常用項目一顆按鈕（名稱＋類型、心率、RPE、段落數、時長），
按下去一次帶入全部設定。沒有合適的才按「空白項目（全部自己填）」。庫是空的時候說明怎麼加（「存成常用」或設定頁新增）。
原本表單最上面的「從常用項目帶入」下拉拿掉（沒有第二個入口）。

**簡易表單**：從常用項目帶入的新項目、課表上「編輯」既有項目，表單上面只有：
- 帶入的是哪個常用項目（新增時可以「換一個」）／既有項目的標題
- 一行「包含」：類型 · 心率 · RPE · N 段訓練段落 · 影片 · 動作——收起來的內容一眼看到
- 時長、距離、備註
- 「更多設定（通常不用改）」收起來：類型、標題、心率、RPE、強度說明、訓練段落、影片、動作，加一句
  「這裡改的只影響這一天；要改常用項目本身，到設定 → 常用項目庫」

欄位只是收起來、還在表單裡，存檔照讀，所以收起來的內容照原樣存回去。存檔被擋（RPE 超出、段落數字不合）時自動打開「更多設定」，
不然錯的欄位看不到。

**完整表單**：常用項目庫的表單（定義項目的地方）、課表上的「空白項目」（沒有來源可以帶）照舊全部攤開。

**順手修的**：教練工具列的「編輯」在手機寬度會折成直排兩個字 → 按鈕文字不換行（圖示給固定大小，不然被擠成 0 寬）。

**挑選那一步照常重畫**：表單開著時同步進來的資料不重畫（第 42 條，怕洗掉打到一半的字），但挑選畫面沒有可以打的東西——
照舊擋的話，剛同步進來、或剛在旁邊按「存成常用」的項目要關掉重開才看得到。所以只有挑選那一步放行，表單照擋。

**對抗式審查抓到、已修的**：常用項目名稱是一長串沒有空白的英文時，挑選按鈕跟「帶入：…」標頭會把手機畫面撐寬，「換一個」被推到畫面外
→ 名稱可以在任何地方斷行；課表卡片的項目標題（舊問題、同一個原因）一起修。

**文案**（同一版，使用者指定）：「產後核心／骨盆底啟動」動作清單底下原本是「推導值：原文第三節模板只寫……這裡的組數次數為推導值，
依身體感覺調整即可。」，改成只寫「組數次數依身體感覺調整即可」。「推導值：」原本是畫面固定加在前面的，改成寫在說明文字裡，
另一份推導的清單（核心／骨盆底進階）照舊顯示「推導值：…」。

**反悔成本**：低。資料格式完全沒動；拿掉挑選步驟、表單回到全部攤開就是原本的樣子。

## 45. 課表上沒有編輯表單：點名稱從項目庫換、只改時間；第 44 條的表單拿掉

使用者回報（2026-09-14，v0.17.0 上線後，附課表「編輯」展開跟項目庫表單兩張截圖）：
「最上面的 Zone 2 跑和文字框，完全不能改動，這是不 ok 的」「請重新整理現在的架構，要刪掉不要的架構，現在被卡死，怎麼改都不是我要的」
「展開就應該要可以選項目庫的內容來變換，不要用目前的框架，太多無用的資訊，還有要改動的框了」。
先出示意圖確認理解後，她的決定：
1. 保留時間的位置可以編輯
2. 保留「存成常用」，而且只要改過、跟項目範本不同，就可以直接存成常用——不用去設定頁加範本再回來
3. 項目庫的表單一起精簡

**第 44 條錯在哪**：「簡易表單」把標題跟一行「包含什麼」畫在最上面、長得像可以點的框，其實不能改；「更多設定」打開又是一整張表單。
兩層疊在一起，看得到的東西大多不能動、能動的藏起來——她要的是「課表只負責挑」，第 44 條還是在課表上做一份表單。

**現在的課表項目卡（教練模式）**：
- 名稱是一顆按鈕（名稱＋▼）。點了在卡片裡展開項目庫清單，每列是名稱＋「類型 · 時長 · 心率 · N 段訓練段落」；
  跟這個項目一模一樣的那列打勾。點一列就把整張卡換成那個常用項目的內容（複製，第 26 條）。**id 不變**——打勾紀錄照 id 對，
  換掉不會讓做過的課又變成沒做（第 0 條）。清單最下面「到項目庫新增或修改 ›」。
- 時間那格是兩個小輸入框（「20 – 30 分」），改完離開輸入框就存。有距離沒時長的（出廠的長跑 17 個是公里）改的是公里。
  休息沒有數字框。範圍 0–300 分、0–100 公里，超出就擋下說明。只填一格＝剛好那個數字。
  下限改得比上限大時，上限跟著變成同一個數字（不是兩個對調）：瀏覽器實測「20–25」把下限改成 30、再改上限成 40，
  對調的寫法會在中間先存成 25–30、下限框變回 25，最後存成 25–40。
- 其餘（心率、RPE、備註、影片、查看動作、訓練段落）照跑者看到的樣子唯讀顯示。
- 工具列：↑ ↓、存成常用、刪除。**沒有「編輯」**。
- 一天最下面「＋ 從項目庫加一個」，一樣是那份清單，挑了加一個新項目（新 id）。

**存成常用什麼時候出現**：這個項目的內容在項目庫裡找不到一模一樣的（兩邊都照存進庫的整理規則比，id、推導值標記不算）。
所以出廠課表的項目、從庫裡換過來又改了時間的，會出現；從庫裡換過來沒動的、剛存過的，不出現。按了直接存，名稱照標題，
不跳視窗，那張卡改寫「已存進項目庫」。名稱可以重複（「Zone 2 跑」20–30 分跟 30–40 分兩份），清單上看時長分得出來。

**今天以前的日子**：卡片全唯讀（名稱不是按鈕、沒有時間框、沒有 ↑↓ 刪除），只留「存成常用」；那天底下寫「今天以前的日子不能改」，
不給「從項目庫加一個」。Store 之外 App 動作裡也再擋一次（換、加、改時間、刪、移都會被擋並說明）。二擇一開關跟這天的備註照舊。

**拿掉的架構**：課表上的項目表單（簡易表單、空白項目的完整表單、「更多設定」、「包含」框）、「編輯」按鈕、
「先挑再填」兩段式（editingItem 狀態）、`saveItemEdit`／`startEditItem`／`startAddItem`／`pickTemplateForNewItem`、
main.js 為表單擋同步重畫的特例（課表卡上只剩時間輸入框，打字中本來就由 isTypingInRoot 擋）。

**項目庫的表單精簡**：
- 「範本名稱」跟「標題」合成一個「名稱」（存檔兩個一起寫）。舊的兩個不一樣的，表單顯示範本名稱。
- 時長、距離、RPE 各一行「[ ] – [ ]」，不再是「下限／上限」四格。
- 「強度說明」拿掉——畫面上沒有任何地方顯示它；存檔保留原本的值（複製出來的照來源）。
- 訓練段落底下那段長說明拿掉。「影片參照」「動作參照」改叫「影片」「動作清單」。

**同一版修的（使用者回報）**：「動作需要動 30 秒，各做 5 組，但我沒辦法同時設定時間以及次數」。
其實存得進去（組數 5、算法選秒數、下限 30），是動作清單編輯器排版害的：一個動作的份量拆成「組數／算法／秒數下限／上限」四格，
她把 30 填進組數（上限 20，存檔被擋），而且新動作預填的「8–10 次」換成秒之後還留著，填 30 會存成「10–30 秒」。
→ 份量改成一行讀起來就是句子：「[5] 組 × [30] – [上限] [秒▼]」；新動作不預填數字；次↔秒切換時清掉數字。

**反悔成本**：中。資料格式沒動（課表項目、常用項目、動作清單都是原本的欄位）；要回到表單式編輯得把拿掉的表單加回來。

## 46. 時間點兩下才改；教練模式不畫當天紀錄；點「第 X 週」切換教練模式；間歇跑也記實際時間

使用者回報（2026-09-14，v0.18.0 上線後，附三張截圖）：
1. 課表裡的時間改成雙擊才可以修改
2. 教練模式的時候，實際時間、體感強度、附註、身體狀況，都可以隱藏（實際分鐘改成實際時間）
3. 加一個小快捷按鈕在本週頁可以快速切換教練模式，快速點擊「第 X 週」可以切換
4. 間歇的反饋除了實際公里，我還需要知道實際用時

**1. 時間點兩下才改**：教練卡上的「20–25 分」平常是文字（底下虛線），點兩下變成兩個輸入框、游標在第一格。
改完離開整組（點別處、按 Enter）收回文字。沒有時間的寫「— 分」，才有地方點。
- 自己判斷兩下（400ms 內點同一個），不靠 dblclick：iOS Safari 對不是連結的元素不一定送 dblclick。第一下不重畫。
- 瀏覽器實測抓到：在輸入框改完按 Tab 跳到上限那格時，存檔觸發的重畫把整組輸入框換掉，游標到不了上限
  → 輸入框開著時存檔不重畫（跟這天備註同一個做法），離開整組才重畫一次。
- 今天以前的日子本來就沒有可以點的時間（第 45 條），兩下也會被擋。

**2. 教練模式不畫當天紀錄卡**：整張紀錄卡（實際數字、沒照表？更換項目／自主休息、體感強度、附註、身體狀況）都不畫——
她列的四塊跟「沒照表」是同一張卡、同一件事（自己的紀錄），只留「沒照表」一行反而怪。項目卡上的打勾圓圈照舊。
關掉教練模式一切照舊。「實際分鐘」改叫「實際時間」，框裡灰字寫「分鐘」（存的還是分鐘）。

**3. 點「第 X 週」切換教練模式**：週次標題本身是按鈕。設定頁的開關照舊。切換只是換畫面、不改資料，誤點再點一下就回來。
v0.19.0 旁邊放了一個小「教練」標籤表示狀態；使用者（v0.20.1）：「可以不用顯示教練按鈕，這是我自己用的隱藏快捷鍵」
→ 標籤、滑過的提示文字拿掉，滑鼠也不變手指，看起來就是一般的標題。開著的時候頂端本來就有教練模式的橫幅。
接著（v0.20.2）：「要避免誤觸，所以要雙擊」→ 跟時間同一個「點兩下」判斷（`App._isDoubleTap`，400ms 內點同一個東西），
點一下什麼都不做。時間跟週次共用同一份判斷，先點時間再點週次不會被當成兩下。

**4. 實際時間**：以前只有課表寫了總時長才給「實際分鐘」，從項目庫換來的間歇跑（只有段落、沒有總時長）只剩公里。
→ 有訓練的日子一律給「實際時間」（休息日照舊不給數字；只寫公里的長跑也多了時間，一樣用得到）。

**反悔成本**：低。資料格式沒動。

## 47. 新增「肌力訓練」類型；類型清單兩邊同步改成可執行的檢查

使用者回報（2026-09-14，附常用項目表單的類型下拉截圖，正在建「腿、跨、臀肌力訓練」）：「加一個肌力訓練」。

**做法**：類型多一個 `muscle`＝「肌力訓練」，排在「重量訓練」前面。跟第 40 條加「間歇跑」一樣只加類型，沒有特殊行為：
不算週跑量（不在 RUN_TYPES）、完成率照一般項目算。「重量訓練」照舊（出廠課表的 strength 不動）。
已經存成「重量訓練」的常用項目不會自動改，要她自己打開改類型。

**順手升級的守衛**（全域原則第 5 條：被依賴的警告不能只是註解）：類型清單在 js/views.js（App 下拉）跟 tools/verify_plan.py
（出廠課表檢查）各一份，以前只靠「⚠️ 兩邊必須一致」的註解。這次加類型剛好要改兩邊——
→ verify_plan.py 直接讀 js/views.js 的 VALID_TYPES、js/store.js 的 RUN_TYPES 比對，漏改一邊就擋下（實測拿掉一邊的 muscle 會失敗）。

**反悔成本**：低。拿掉類型＝下拉少一項；已經用了 muscle 的項目會顯示成「類型已停用，請改選」（第 12 條的 walk-run 同一條路）。

## 48. 階段目標的跑量拆成「目標／預計／實際」三行；總覽進度加平均週跑量

使用者回報（2026-09-14，附總覽「階段目標」截圖：「跑量目標 km [58.9]–[73.4]；課表這階段大約 58.9–73.4 km」）：
「這裡的跑量目標，應該是預計跑量。這裡我們改成三段：目標跑量（教練可以填，沒有任何連結的數值，就是一個可以看的目標數字）、
預計跑量（跟課表聯動）、實際跑量（跟學員填寫實際跑量公里聯動）」「另外總覽加一個平均週跑量在進度裡」。

**原本為什麼看起來像「預計」**：跑量目標的輸入框沒填時，灰字（placeholder）是課表這階段的加總，底下又一行「課表這階段大約…」，
整塊讀起來就是課表的數字。

**三行**（第 22 條的延伸——資料沒動，`phaseTargets` 的 `volumeKm` 還是教練填的目標）：
- **目標跑量**：教練填的兩格，沒有灰字、不跟課表比、不提醒「比課表高」、不跟實際畫進度條。唯讀的人看得到數字。
- **預計跑量**：課表這階段每週自動目標的加總（`phaseVolumeAutoRange`，教練改過的課表也算進去），唯讀。
- **實際跑量**：這階段已填的實際公里加總（`phaseVolumeActual`，跟週跑量同一套算法），沒有任何紀錄寫「—」，唯讀。

拿掉的：輸入框灰字、「課表這階段大約」提示、「你的目標比這高」、「本階段累積 X / 目標」進度條——都是把目標跟別的數字連起來的東西。
第 22 條當初要的「學員看得到累積跑量跟目標的差異」，三行上下並排直接看得到。沒設定任何目標時，唯讀的人照樣看到預計跟實際兩行，
底下寫「教練還沒設定這階段的目標」。

**平均週跑量**：總覽「我的進度」多一格（四格改成 2×2）。只算**已經過完的週**、而且**有填實際的週**：
這週還沒過完，算進去會把平均往下拉；沒有紀錄的週不當成 0（第 0 條：沒資料不是做了 0）。格子下面寫算了幾週，沒得算寫「—」。
看別人的進度時算的是那個人的。

**反悔成本**：低。資料格式沒動。

## 49. 本週的「目標跑量」是教練自由訂的數字（取代第 13 條「只能往下調」）

使用者回報（2026-09-14，手機截圖）：「底下本週課表設定的部分基本上完全動不了，被連動數字卡死，教練應該可以設定一個不被連動卡死的目標」。

**卡死的原因**：第 13 條的「週跑量目標」是課表加總的天花板——只能往下調，寫入時擋「上限不能高於課表加總」，讀取時再夾一次。
教練想訂一個比課表高（或跟課表無關）的目標數字，永遠存不進去。

**做法**（跟第 48 條階段目標同一個想法）：
- 教練模式「本週課表設定」：「目標跑量（K）」兩格自由填（0–200），沒有課表灰字；底下唯讀一行「預計跑量＝課表跑步項目加總」；
  「設定目標跑量」「清除目標」。說明：目標不會限制或改變課表，也不影響進度條。
- `weekVolume` 的 `target` 一律是課表加總（預計）；教練的數字另外回傳 `goal`，原數字、不夾。
- 本週頁頂端的卡：「實際 / 預計」跟進度條不變，有目標時多一行「目標跑量 X–Y km」。

**第 0 條的防線留在課表那邊**：進度條滿格＝預計下限、「已超過本週課表上限——下週不要再加」照預計上限算——
都不看教練的目標。目標是看的數字，不是催人跑滿的條（第 22 條使用者的判斷：目標數字不用連動或限制）。

**資料**：沿用 `planOverrides/{週}.weeklyVolumeKm`，舊資料（以前存的都 ≤ 課表加總）直接變成目標跑量。

## 50. 教練模式拖曳改成「兩天對調」，這週也可以（取代第 43 條「只限還沒開始的週」）

使用者回報：「教練模式還是無法直接把兩天的課表調換，這個跟學員模式不同，學員模式是調換，教練是直接切換來避免重填兩格」。

**做法**：教練模式按住 ⋮⋮ 拖到另一天上放開，兩天的共用課表**對調**——只有這兩天變（第 43 條是插入，中間的日子會跟著移一格）。
拖曳中列不移動，落點那一列虛線框亮起來；放開時以手指／滑鼠底下那一列為準。SortableJS 的 Swap 外掛不在 cdnjs 那個 build 裡，
用 onMove 回 false（不重排）＋ onEnd 查落點自己做。一般模式照舊是自己的順序（插入）。

**今天以前的日子**鎖住：沒有把手、不能當落點，App 動作裡再擋一次。**這週今天以後的日子可以換**（第 43 條整週鎖住的限制拿掉）。
牽涉到**今天**時先確認：「今天已經練過的人，打的勾會留在今天，換走的那堂課會在另一天顯示成還沒做——不要再做一次」
（打勾紀錄是日期＋項目 id，第 43 條的分析還是對的；使用者要能換這週，就把風險講在按下去之前）。

**接受的缺口**：有人在這週自己對調過（個人 dayOrder 指到會被換的共用天），他畫面上那兩格內容會跟著變。三個人、同週、同一天都碰到才會發生。
換天照舊寫 `layoutAt`，別台停在舊排列的畫面不會照位置存（第 45 條）。

## 51. 登入後「我是誰」固定是登入的帳號；晚回來的寫入結果不蓋現在的身分

使用者回報（附「寫入被拒：這個 Google 帳號不能寫入「Annlin」的紀錄」截圖）：「我在切換人員後切換回我個人，但是系統並沒有跳回來，導致我沒辦法改我要改的內容」。

**原因**（兩個）：
1. 登入之後設定頁照樣能把「我是誰」切成別人。Firebase 規則只准本人寫自己的紀錄，切成別人之後每筆寫入都被拒——
   切過去的那一刻，本機留著的那個人的紀錄也會補推一次，全部被拒。
2. 寫入被拒是伺服器晚一點才回來的。切成 Annlin 又切回 Mick，Annlin 那幾筆的「被拒」這時才回來，
   把 Mick 的狀態蓋成「寫入被拒」（訊息寫的是 Annlin），而且那些日期被記成失敗、要等同一天重寫成功才會解除——看起來就是切不回來。

**做法**：
- 登入、而且判斷得出是誰（第 41 條）時，設定頁其他人變淡、點了說明「登入的帳號是 X，只能記自己的紀錄；要看別人的進度到總覽 → 查看別人的進度」。
  判斷不出來（離線）或沒登入時照舊可以切。
- Sync 每換一次身分計數加一；自己的寫入回來時身分已經換過，就不動狀態、不記失敗。幫別人寫的（教練寫別人的目標，第 15 條）不受影響。
- 已經卡住的：重新打開 App 時第 41 條會自動選回登入的帳號。

## 52. 改常用項目會套用到今天以後用到它的課表（取代第 26／45 條「帶入是複製、改範本不影響排好的日子」）

使用者回報（2026-09-14）：「我剛剛在 Zone 2 跑跟間歇跑裡加了熱身影片，但系統沒有更新到本週課表裡……照理說這個更改應該要 apply 進去，因為屬於今天之後」。
跟動作清單／影片「改了從今天起生效」（第 33 條）同一個規則，常用項目以前是例外（複製式），現在一致。

**哪些課表項目算「用到它」**：
- 從項目庫換進來、加進來的：記 `templateId`（這版開始），改名也對得到。
- 還沒連的（出廠課表的、這版以前換進來的）：名稱跟類型一樣就算。連著別的常用項目的不算（兩份同名的常用項目不會互相影響）。
- 只看今天以後（含今天）的日子；今天以前的日子不動。套過的項目會記下 `templateId`。

**只套「這次改了的地方」**：查了出廠資料，「Zone 2 跑」有 28 天在今天之後，備註分三種、時間分四種——整份蓋過去會把各週的備註跟時間洗掉。所以：
- 類型、名稱、心率、RPE、備註、動作清單、訓練段落：這次有改的欄位才套。
- 時間／距離：項目的數字還是舊的常用項目那個數字才跟著改；某天單獨改過的（第 46 條點兩下改的）不動。
- 影片：這次拿掉的從項目拿掉；常用項目有、項目還沒有的補上——**之前已經存過、當時沒套上的修改，打開那個常用項目再按一次「儲存」就會補上**（常用項目沒有保留舊版本，只能這樣補）。

**畫面**：常用項目的表單最上面寫「存檔後，今天起用到這個項目的 N 天會跟著改」；存完項目庫上方寫「已套用到今天以後的 N 天課表」。
套用是整週存（每個有改到的週一份共用課表修改），跟教練改課表同一條路（版本比對、搬過天的畫面不照位置存）。

**反悔成本**：中。已經套上去的內容不會自動退回（課表是整週存的）；要撤回就把常用項目改回去再存一次，或到那週「還原本週為出廠預設值」。

## 53. 「在看誰」跟「我是誰」分開：唯讀看別人每天的紀錄

使用者（2026-09-15，先討論）：「有沒有辦法可以看其他人的進度，不要鎖？這樣教練看不到他每天的訓練紀錄了」。
第 51 條把「我是誰」鎖成登入的帳號之後，教練沒有地方看別人每天的紀錄（總覽只有完成率、跑量這些加總）。
討論後她決定：本週頁跟總覽都要有入口；看別人時照建議——一律顯示紀錄、收起改課表的工具。

**為什麼不是把身分解鎖**：「我是誰」決定寫進誰的紀錄，切成別人每一筆寫入都會被規則拒絕（第 51 條那個卡住的狀況會回來）。
看別人是另一件事，不需要寫入。

**入口**：
- 本週頁「本週訓練目標」前面的名字（第 52 條之後加的）登入後可以點開：自己（可以打勾、記紀錄）／其他人（看每天的紀錄，唯讀）。
- 總覽看別人的進度時，最上面多一顆「看 Annlin 每天的紀錄 ›」，跳到本週頁。
- 「在看誰」是同一份狀態（`App.state.viewingUserId`，總覽本來就有），兩頁一致。

**看別人時的本週頁（唯讀）**：
- 「本週訓練目標」前面的名字變成「正在看 Annlin・唯讀 ▾」（橘色，跟平常的名字分得出來），點開選單第一列「Mick（自己）· 回到自己」。
  （v0.24.0 是上方另一整條「正在看 Annlin 的紀錄（唯讀）」橫幅＋「回到自己」按鈕；使用者：「把上面欄的正在看精簡後，整合到下面 Annlin ▾ 的位置」，v0.24.1 併進名字。）
- 每一天照**那個人自己的順序**（她對調過的日子）、那個人的狀態；展開是唯讀卡：課表項目＋她打的勾、當天狀態、實際時間／公里、體感、附註。
  沒有任何輸入框或會寫資料的按鈕。表格模式也是她的紀錄。跑量卡、本週回顧也是她的（本週回顧不給「標記本週已降量」）。
- **身體狀況看不到**：漏尿／疼痛／過度疲勞跟私人備註只有本人讀得到（Firebase 規則），唯讀卡只寫一行「身體狀況只有本人看得到」。
  要讓教練看得到得改規則、而且要本人同意——使用者沒有要求，維持私密。
- **改課表的工具收起來**（拖曳把手、名稱選單、時間、本週課表設定、教練模式橫幅）：課表是三人共用的，在「看 Annlin」的畫面改，
  很容易以為只改到 Annlin 的。要改就「回到自己」，教練模式的操作（點名稱換項目、點兩下改時間、拖曳對調）完全照舊。

**順手修的**：`effectiveDayOrder` 在教練模式一律回傳出廠順序，連「別人的」順序也被蓋掉——總覽看別人的完成率時，
她對調過的日子會對到錯的課。改成教練模式的出廠順序只套在自己（編輯情境），別人一律用她自己的順序。

## 54. 教練備註：每個課表項目可以寫一句「這一天」的提醒，沒寫就不佔位置

使用者（2026-09-21）：「我想在教練排課程的地方加一個備注，有打的時候會出現在課表，沒打就不出現空格」。

**為什麼是另一個欄位，不是改項目本身的說明**：項目的說明（「心率壓在 Zone 2……」）跟著常用項目走——第 52 條之後，
改常用項目會套用到今天以後用到它的每一天。「今天改跑步機」寫進說明，下次改常用項目時會被蓋掉，或反過來被套到別天。
所以「這一天、這個項目」的提醒另外存在課表項目上（`coachNote`），常用項目不碰它。
「這天的備註」（`dayNotes`，整天一則的藍色提示，二擇一的說明用）照舊。

**怎麼寫**：教練模式展開那天，項目下面的按鈕列多一顆「＋ 備註」（有備註時是「改備註」）；點了在項目裡打開輸入框，
打完點別的地方就存，清空＝拿掉。最多 200 字。今天以前的日子不能改（跟改時間、換項目同一個鎖）。

**怎麼顯示**：有寫才出現——項目的數字下面一行淺橘底「教練備註 ……」；沒寫就什麼都沒有，不留空格。
學員的卡片、教練模式、唯讀看別人（第 53 條）都看得到。

**跟其他功能的關係**：
- 點名稱從項目庫換成別的項目（第 45 條）：備註留著（它是給這一天的）；要拿掉就「改備註」清空。
- 「存成常用」不會把備註存進項目庫；有沒有備註也不影響「存成常用」按鈕出不出現。
- 改常用項目套用到課表（第 52 條）不動備註。
- 跟其他教練模式的修改一樣整週存（版本比對、搬過天的畫面不照位置存）。

**反悔成本**：低。多一個選填欄位，不填就跟以前一樣。

## 55. 休息日可以排「選做」的恢復運動；那天還是休息日

使用者（2026-09-21）：「想要休息日但是做伸展，所以希望休息日還是可以加一些回復型運動」。

**怎麼算「休息日」**：這天有「休息」項目、其他項目都是恢復類（`recovery`：伸展、散步、骨盆底這些），而且不是二擇一。
教練在休息日「＋ 從項目庫加一個」加伸展，這天就是「休息日＋選做的伸展」。
二擇一的「休息 或 伸展」照舊是二擇一，選一個就算完成。

**為什麼是「選做」**（第 0 條：休息不能變相加量）：伸展加進休息日，如果照一般的日子算，沒做伸展這天會變成「未完成」、
拉低完成率——休息日反而多一件要交差的事。所以：
- 恢復項目旁邊標「選做」，做了打勾就好。
- 這天不算進完成率（跟純休息日一樣從分母拿掉）；沒做不會出現「未完成」，過去的日子不會標紅。
- 還沒勾的時候狀態寫「休息日」（不是「待完成」）；勾了才寫完成。
- 「完全休息」那一行不用勾。
- 休息日不放「沒照表？更換項目｜自主休息」（第 13b 條：休息日不遞出「用訓練取代休息」）。

**只認恢復類**：教練在休息日加的是跑步、肌力這些，這天就不算休息日，照一般的日子算——那是明確要排訓練，
不是「休息日順便伸展」。

**反悔成本**：低。只影響「休息＋恢復類」這種組合的顯示跟完成率。

## 附：日期修正本身

| | 原本 | 修正後 |
|---|---|---|
| 起算日 | 2026-09-10（**週四**） | 2026-09-07（週一） |
| 總長度 | 179 天 = 25 週又 4 天 | 182 天 = **26 週整** |
| 比賽日位置 | 構不到第 26 週 | 第 26 週第 7 天 |
| Phase 4 | 「2/11-3/1」= 19 天（宣稱 3 週） | W23-25 = 21 天 |
| 賽週 | 「3/2-3/7」= 6 天（宣稱 1 週） | W26 = 7 天 |

原始計畫所有階段邊界都落在週四，但每週課表模板是按「一二三四五六日」排的。
兩者擇一時**以模板為準**（週一起算），因為模板是實際要照著練的東西，階段邊界只是分類標籤。

完整的週次對照見 [日曆基準.md](日曆基準.md)。
